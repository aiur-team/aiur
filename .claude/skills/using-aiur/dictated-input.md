# Dictated Aiur input

Voice-originated text may render **Aiur** as `aeor`, `iyer`, `ayer`, `A, your`,
`IR`, `iron`, `higher`, or `ire`; treat those as possible dictation errors in
Aiur-specific context rather than automatically taking them literally, while
never silently rewriting a real word or acronym.
