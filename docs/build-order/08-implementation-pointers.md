# Implementation pointers (researched at 9849f329)

Concrete per-ticket anchors for worker agents: exact files, modules, functions,
data shapes, and patterns verified against `origin/main` at the pack's
researched commit. This is the prescriptive layer the abstract ticket
contracts intentionally omit; treat every path/name here as a refreshable
note pinned to `researched_at_commit` — re-verify at pickup, but start here.

Worker instruction: read your ticket's section below BEFORE resolving the
ticket's cross-references. All application code lives under `src/` (mix
project root), not the repository root.

Coverage: 35 of 53 tickets have researched pointers below (the deep-read set).
Not yet covered: BO-008, BO-011, BO-015, DASH-006, DASH-010, DASH-012,
DASH-013, DASH-015, DASH-017, DASH-018, DASH-019, DASH-020, DASH-021,
DASH-023, DASH-024, DASH-025, DASH-026, DASH-033 — extend this file the same
way (verify against the researched SHA) before dispatching those tickets.


## Cross-cutting gotchas (verified absences and traps)

These are things tickets reference that DO NOT EXIST on the researched
baseline, or existing behavior that will silently break naive work:


### Dashboard web layer

- No CSP header exists anywhere (endpoint.ex/router.ex only call put_secure_browser_headers, which does not emit Content-Security-Policy). BO-009/BO-012 speak of 'current CSP' compatibility as if one exists — a cheap model may hunt for a CSP config that isn't there. Tickets should say 'no CSP exists today; do not require one / add one explicitly'.
- No static-asset cache busting exists: StaticAssetController serves fixed URLs with cache-control public,max-age=31536000 (static_asset_controller.ex:31). BO-009 requires cache-busting compatibility but there is no digest/versioning mechanism to be compatible WITH — the ticket needs to state the mechanism (e.g. version-segmented URL paths).
- No JS build/bundling pipeline exists ('the repository's reproducible asset pipeline' in BO-009 is aspirational): all dashboard JS is inline in layouts.ex; vendored Phoenix JS is embedded at compile time from dep app dirs. The compile-time File.read! embedding pattern in static_assets.ex is unsuitable for a large layout engine — tickets should point at the runtime-read aiur-logo.png pattern instead.
- No dialog focus-trap/Escape/focus-restore helpers exist despite BO-013/BO-018/DASH-027 saying 'refresh current dialog/focus helpers'. AgentLogModal only has phx-click-away. Each of these tickets needs 'build the focus lifecycle hook (none exists)' stated explicitly, or one ticket must own creating the shared helper.
- No route registry / navigation seam exists: BO-012 says 'register the real route through the current route-aware dashboard navigation seam' but the seam is DASH-001's deliverable; on main the nav is two hardcoded links in Overview.tabs/1. If BO-012 lands before DASH-001 the executor must edit Overview.tabs directly — worth stating as the fallback.
- No browser/a11y/perf test harness exists (no wallaby/playwright in mix.exs; LiveViewTest+floki only). Every web ticket's browser-test gate depends entirely on BO-008 landing first — correct as a dependency, but nothing on main can be reused or extended for touch/zoom/screenshot assertions.
- trusted_url/1 is triplicated (fleet_table.ex, decision_detail.ex, recent_outcomes.ex) rather than being a shared 'trusted-link policy' module; tickets that say 'reuse the trusted-link policy' should name one of these functions to copy or direct extraction into a shared module.
- Fleet filter state is socket-only, not URL-backed (toggle-fleet-filter mutates a MapSet assign; only the decisions filter uses push_patch/handle_params). DASH-003/DASH-016's URL-state requirement is a new mechanism for the fleet surface, with decision_path.ex as the only in-repo precedent.
- Banner/tab decision counts on main come from the bounded newest-50 DecisionStore window (recent_decisions(50) in control_center_presenter.ex:33) — DASH-007's 'canonical retained counts' provider (DASH-006) does not exist yet; until it lands, any count copy must keep the bounded-window caveat.
- DASH-016/DASH-003/DASH-027 backend inputs (DASH-002 membership, BO-005 activity, DASH-026 transcript snapshots, BO-016/BO-019 detail/history snapshots) have no counterpart on main — the web tickets correctly depend on them, but a cheap model should be told there is NO interim data source (the only transcript source today is filesystem reads via Aiur.AgentLog, which the tickets forbid).
- dashboard.css is embedded at compile time via @external_resource (static_assets.ex:9,14) — editing CSS requires recompiling the app to see changes; worth a one-line gotcha in every CSS-touching ticket.
- BO-012 names routes /build-orders and /build-orders/:root_number — any added route must be placed before the catch-all `match(:*, "/*path", ..., :not_found)` scope at router.ex:135-144 or it will 404; no ticket mentions the catch-all.

### GitHub planning-graph backend

- No module named Aiur.EventBus exists — BO-019 names it as a reuse target; the real seams are Aiur.Events.Exchange / Publisher / SubscriptionStore plus Aiur.IssueLog. A cheap model grepping for 'EventBus' finds nothing and may invent a new bus.
- StatusReport is not a struct and has no test factories: Aiur.Orchestrator.StatusReport builds plain maps inline (running_snapshot/retry_snapshot/idle_issue_snapshot). BO-004's 'StatusReport structs/APIs/PubSub ... test factories/serialization seams' overstates the existing seam; the work is adding a key to three map builders and their orchestrator tests.
- GitHub.Config.repo/0 silently falls back to Aiur.Git.origin_repo() (auto-detected from the local git remote) when github.repo is unset — BO-004's 'trusted tracker configuration' identity can today derive from exactly the local-workspace source its invariants forbid; the ticket never says what identity state an auto-detected repo yields.
- No state_reason (COMPLETED/NOT_PLANNED) capture exists anywhere in the codebase, and normalize_issue discards node_id — BO-001/BO-002's cleared-vs-terminal_unsatisfied semantics and node-ID joins require net-new fetch fields, correctly implied but nowhere stated as 'these fields are absent today, add them'.
- No last-known-good / health-aware cache pattern exists to reuse: BO-003's 'reuse Aiur's cache and provider-health patterns' points at AiurWeb.ControlCenterCache (simple TTL read-through, no generations/LKG/health) — the LKG projection is a from-scratch build; the reusable parts are only OTP supervision, Aiur.TaskSupervisor, and Phoenix.PubSub.
- No GraphQL issue-graph queries exist today (only viewer-login and reviewThreads) — BO-002's 'native sub-issue/dependency API knowledge' in-repo is limited to the REST DependenciesApi (blocked_by/blocking, api_version 2026-03-10); parent/subIssues GraphQL is unexercised and must be schema-introspected at pickup, as the ticket does say.
- Tickets contain zero concrete file paths or module names from the current tree (by design — 'refreshable implementation notes'), which directly conflicts with the operator's cheap-model concern; the ticket_pointers above are the missing prescriptive layer and should be attached to the GitHub issues at dispatch.
- BO-019's 'never parse raw log files' phrasing collides with the fact that the supported IssueLog query APIs (event_history/2, disk_history/2) are themselves parsers over the daemon's structured log files — a literal-minded implementer may refuse the correct reuse target; the prohibition should be scoped to agent transcripts/NDJSON/terminal output in LiveView.
- BO-017's three named producers ('progress, active-agent-stage, latest-safe-evidence') do not map one-to-one onto code: progress = ticket.<id>.agent.progress via tool_executor.ex; 'active-agent-stage' has no single producer today (stage lives in AgentPubSub broadcast_status_change + orchestrator work_state, not the Exchange); 'latest-safe-evidence' is closest to alerts.ex emissions — the producer inventory the ticket demands will have to define these, not merely migrate them.

### Usage/cost accounting family

- Cache-creation/cache-read/reasoning token dimensions are captured NOWHERE today: TokenUsage.canonicalize drops everything but input/output/total, and Payloads/EventNormalizer only extract those three. DASH-008/011/029's additive/subset relationship machinery assumes the installed Codex/Claude headless protocols expose these dimensions — plausible, but there is zero fixture or code evidence in the repo, and the tickets defer it to 'characterize at pickup'. If the installed protocols don't expose cache dims, the Claude-additive/Codex-subset fixtures in DASH-008/011 acceptance become synthetic-only and DASH-029's mapping shrinks to input/output/total.
- No OpenTelemetry/OTLP code exists anywhere in src/lib — DASH-019 is 100% greenfield including a possible aiur-claude sibling launch change behind GATE-CLAUDE-OTEL-PROTOCOL-AUTHORITY. DASH-010 and DASH-013 are likewise gated. DASH-031 hard-depends on DASH-010 AND DASH-013, so the entire user-visible payoff of the 17-ticket family is transitively blocked on two human protocol gates; the pack defines no codex-only interim milestone.
- DASH-013's acceptance ('all Claude meters unsupported is not completion') is unsatisfiable from current repo evidence — no structured Claude quota/plan-tier source exists in the codebase at all. The gate acknowledges this, but it means DASH-013 is non-dispatchable to a cheap model until a human resolves the gate, and its complexity:4/model:codex label is misleading as-is.
- DASH-018's Claude side has no trusted auth-lifecycle signal today (Codex has account/updated authMode + chatgptAuthTokens/refresh events, currently only humanized for logs; Claude has nothing). Claude account generations will be permanently 'unknown' until DASH-019 — permitted by the ticket's unknown semantics but never stated, so a cheap model may burn effort hunting for Claude lifecycle evidence that does not exist.
- The exact-decimal-money invariant (DASH-008/010/011/029) collides with the actual decode path: Jason decodes JSON numbers to binary floats at the port, and claude cost_usd already flows through put_if_number as a float (claude/coding_agent.ex:393). No ticket names the decode site (codex/app_server_port.ex, claude port read) where re-extraction from the raw string must happen — a cheap model will silently violate the invariant.
- Per-envelope requested_model/resolved_model/effort/auth_mode attribution is not captured per-event today; it depends on BO-017 (outside this family). Early envelopes will be largely attribution-unknown, which the schema tolerates but which guts the per-model/per-ticket cost value the operator asked for until BO-017 lands — the family's real critical path runs through a ticket it doesn't contain.
- Ticket prose is constraint-dense but pointer-sparse: across all six deep-read tickets there is not one file path, function name, or line reference into current code (only module names like TokenAccounting, MessageHandler, Boot.run_id). Given the operator's cheap-model concern, this family needs the pointer injection above more than any other area — the invariant prose (fail-closed, revisions, generations, epochs) vastly outweighs implementation guidance.
- Over-decomposition is mostly load-bearing (storage chain 009/024/025 with a destructive-phase split; contract-vs-adapters 008/029 and 012/020/013; auth boundary 021) but two merges are defensible: DASH-011+DASH-030 (two pure libraries over already-pinned contracts) and DASH-015+DASH-031 (two protected card sets on the same Units page sharing the same serialization set). Conversely DASH-008 is the family's riskiest cheap-model assignment: a pure abstract schema/registry ticket with property-test-heavy acceptance and no real fixtures to anchor on.
- Minor: tickets repeatedly say 'reuse atomic/checksummed file conventions' — the existing precedents (JsonStore, DecisionLog) do fsync+atomic-rename and validator-based replay but store no checksums; the checksum requirement is new work, not reuse.
- Minor: the pack's 04 doc says 'Aiur has no application Ecto repository' — true (no use Ecto.Repo), but ecto 3.13 IS a dependency (used repo-less), which conveniently makes Decimal available for the exact-money work; no ticket mentions this.

### Runtime truth: slots/capacity, run membership, lifecycle events

- DASH-028 backend: verified the pack is RIGHT that no backend work is needed — Slots read/adjust/set + authoritative status map all exist on main. Residual UI-side gaps only: no capacity fact is currently in the dashboard payload (implementer adds a Slots.max_concurrent_agents/0 read), and the status map lacks a literal `source` field ('configured/source/session-override' wording overstates the map by one field; configured + session_override? are what exists).
- Repository-qualified ticket identity does not exist anywhere in shipped runtime events or StatusReport — topics are keyed by bare ticket numbers (event_intake.ex extract_ticket_id) and StatusReport rows carry bare `identifier` strings. Every ticket in this area that 'consumes BO-004/BO-017 identity' is blocked on genuinely greenfield work; the pack declares this correctly, but cheap-model implementers should be told explicitly there is no existing identity module to find.
- BO-005 transport gotcha not spelled out in the ticket: today's AgentList activity fold rides Aiur.Events.DebugLog (topic aiur:events:debug), whose moduledoc claims it is debug-only and only subscribed under --debug, but Aiur.AgentList.App.subscribe/1 (app.ex:243) subscribes unconditionally — the moduledoc is stale. The new supervised projection must choose a first-class subscription (Exchange/SubscriptionStore) rather than inheriting the debug mirror; the ticket says 'reuse existing event/PubSub vocabulary' without warning about this trap.
- DASH-014 assumes run start 'may not be retained centrally' and plans a contingency — unnecessary: Aiur.Boot.started_at/0 (wall clock, paired monotonic mark) already exists on main at the researched SHA.
- DASH-004: the ticket implies no acknowledgement machinery exists; actually request_id minting ({:pause_agent, request_id} via OperatorMessages.send_running_control_message) and a pause ack carrying request_id ({:worker_control_state, _, :paused, %{request_id: _}}) already round-trip — what's missing is (a) request_id on the :working/resume ack (3-tuple, message_handler.ex:170) and (b) any correlation in handle_worker_control_state. Implementers should extend this envelope, not invent one.
- Gate inconsistency inside the pack: DASH-005/028/032/034 mark GATE-OCC-PREDECESSOR-BASELINE resolved at origin/main@9849f329, while DASH-002/004/014/022 in the same dependency chains still say 'resolve before dispatch' for the same baseline — should be normalized before publication or cheap models will stall on a phantom gate.
- DASH-002's reuse target 'daemon-private state root' resolves to Config.Paths.decision_state_dir() — a decision-named path helper; the membership journal needs its own path helper rather than colonizing the decisions directory (minor, but a cheap model will otherwise drop journal files under decisions/).
- No SCC/Tarjan/topological/adjacency helpers exist anywhere in main — BO-007's graph module is fully new (the ticket's 'use pure Tarjan helpers' reads like reuse but is a build instruction).
- DASH-032 run-window end: no ended-run/end-time concept exists in Boot or anywhere else; the ticket correctly scopes to the active run only, but its 'future ended-run query' hedge has no existing anchor.
- BO-006 render gotcha absent from the ticket: Aiur.AgentList render pipeline only shows fields explicitly threaded through RenderState.build/1 (app.ex:295-303 comment) — swapping activity sources without updating render_state.ex silently renders nothing; this is a known recurring project bug class and should be added to the ticket's implementation notes.

## Per-ticket pointers


### BO-001

Pure domain contract — no I/O. READ FIRST: src/lib/aiur/issue.ex (Aiur.Issue struct — do NOT add graph fields to it), src/lib/aiur/github/labels.ex (Aiur.GitHub.Labels: label_set/2, state_labels/1, marker_suffix?/1 — the existing label vocabulary), src/lib/aiur/coding_agent.ex:366-384 (complexity_level/1 — the duplicate-tolerant Enum.max parser your strict parser must NOT copy; a planning parser returns an explicit ambiguity warning on duplicates instead), src/lib/aiur/github/issue_state.ex (Aiur.GitHub.IssueState — label-swap state machine; note it has NO state_reason concept). Create new modules under a bounded namespace, e.g. src/lib/aiur/build_order/ (Aiur.BuildOrder.Member, .Identity import from BO-004, .EdgeState, .Readiness, .Catalog, .Diagnostics). Data shapes: edge_state is exactly one of :cleared | :blocking | :terminal_unsatisfied | :unknown | :cyclic; readiness precedence cyclic > unknown > terminal_unsatisfied > blocking > ready; GitHub lifecycle facts arrive as state ("open"/"closed") + state_reason ("completed"/"not_planned"/nil) — neither exists in Aiur.Issue today, so define them fresh on your member record. Complexity/phase/lane labels: parse 'complexity:1..5', 'phase:N' (positive int), 'build-lane:*' — return {:ok, val} | :missing | {:ambiguous, values} rather than max/first. Error convention to copy: tagged tuples like {:github, class, detail} (src/lib/aiur/github/errors.ex). Tests: mirror src/test/aiur/github/issues_test.exs style (pure fixtures, no HTTP). Gotcha: BO-004 must land first — import its identity type; do not define your own {owner, repo, node_id} tuple.


### BO-002

New read adapter, e.g. src/lib/aiur/build_order/github_graph.ex (or github/build_order_graph.ex). REUSE: Aiur.GitHub.Transport.github_graphql/4 (transport.ex:104 — POSTs %{query, variables} to https://api.github.com/graphql, returns {:error,{:github_graphql_errors,errors}} on partial data — note it REJECTS any response with errors, which matches your fail-closed contract), Transport.parse_repo/0 + require_token/1, Aiur.GitHub.Errors.classify_error/1 + github_status_error/1 (errors.ex — auth/rate_limited/timeout/dns/tls/http taxonomy already exists). COPY THE PATTERN from src/lib/aiur/github/review_threads.ex:38-70 — module-attribute GraphQL query with pageInfo{hasNextPage endCursor}, cursor variable, paginated loop, and request_fun injection via opts for tests (every github module does `Keyword.get(opts, :request_fun, &Transport.default_request_fun/1)`). GraphQL fields you need (introspect at pickup): repository(owner,name){ issue(number){ id databaseId number title state stateReason labels(first:100){nodes{name}} parent{id} subIssues(first:100){pageInfo nodes{...}} blockedBy(first:100){...} blocking(first:100){...} } }. REST fallback: copy Aiur.GitHub.DependenciesApi.dependency_get/3 (dependencies_api.ex:61 — note the api_version key '2026-03-10' in the request map; Transport.github_headers switches header on it). DO NOT reuse PR #1012's hydrate_blocked_by (open PR on issues.ex): it is per-issue N+1 and returns [] on fetch error — your contract forbids both. Config: add bounds to the YAML schema — copy src/lib/aiur/config/schema/tracker.ex (Ecto embedded_schema + changeset with validate_number) and thread through Aiur.Config.settings!(). Normalization target: BO-001 candidate records; keep Issues.normalize_issue/4 (issues.ex:183) untouched. Tests: request_fun-stub fixtures as in src/test/aiur/github/ — return canned %{status: 200, body: ...} maps.


### BO-003

New supervised projection, e.g. Aiur.BuildOrder.GraphProjection (GenServer) + optional Task usage via {Task.Supervisor, name: Aiur.TaskSupervisor} (already in tree). REGISTER in the supervision list: src/lib/aiur.ex child_specs/1 (:96-142) — insert near Aiur.Events.Publisher/Aiur.DecisionStore; this exact list is the seam BO-005/BO-016/BO-019 also edit, hence the serializes-with. Off-process build + atomic swap: build candidate in Task.Supervisor.async_nolink, validate fully, then replace state in one handle_info. PubSub: broadcast via Phoenix.PubSub on Aiur.PubSub — copy the wrapper style of Aiur.AgentPubSub (src/lib/aiur/agent_pubsub.ex: topic constants + Process.whereis guard in do_broadcast, :151) or AiurWeb.ObservabilityPubSub (src/lib/aiur_web/observability_pubsub.ex — single '@topic observability:dashboard' + broadcast_update/0); define e.g. 'build_order:catalog' and 'build_order:graph:<root_node_id>' topics. Do NOT use Aiur.Events.Exchange for this — that is the cross-ticket agent event bus with contamination filters (publisher.ex:89), wrong layer for cache-generation notifications. What NOT to extend: AiurWeb.ControlCenterCache (control_center_cache.ex — read-through TTL fetch/4 with loader fun, no LKG/health/generations; useful only as a shape reference). Deterministic timers: accept :refresh_interval_ms / clock via start_link opts (many modules do this; see Publisher.init ttl_ms pattern, publisher.ex:188-201). Config defaults 60s/15s/5s: add a Build Order section next to src/lib/aiur/config/schema/polling.ex and copy its changeset validation. Restart semantics: in-memory only, GenServer init returns :unavailable state; no persistence exists to copy — do not invent one.


### BO-004

Files to touch: src/lib/aiur/issue.ex (add one field, e.g. tracker_identity: nil, to defstruct + @type — GOTCHA from memory: any new field consumed by AgentList render must be threaded, but this one is backend-only), src/lib/aiur/github/issues.ex normalize_issue/4 (:183 — currently ignores _owner/_repo and discards gh_issue["node_id"]; extract all three here: %Identity{tracker: :github, owner: owner, repo: repo, node_id: gh_issue["node_id"], number: number}), src/lib/aiur/orchestrator/status_report.ex (running_snapshot/4 :141, retry_snapshot/3 :191, idle_issue_snapshot/3 :236 — add :identity key to these three plain maps; they are NOT structs and there are no factories), and the GraphQL/REST detail paths that also build issues (Issues.fetch_issue_raw returns the raw map — normalization happens at callers). Identity type: new small module e.g. Aiur.TrackerIdentity or Aiur.BuildOrder is wrong — keep it tracker-generic (it's consumed by BO-017 events too); place beside src/lib/aiur/tracker.ex / tracker_config.ex. Config source of truth: Aiur.GitHub.Config.repo/0 (src/lib/aiur/github/config.ex:11) — CRITICAL GOTCHA: it falls back to Aiur.Git.origin_repo() (git.ex:146, auto-detect from local remote) when github.repo is unset; decide and test whether auto-detected repo counts as 'configured' or yields the absent/unjoinable identity state; also Transport.parse_repo/0 (transport.ex:19) is the other reader of the same value. Schema: src/lib/aiur/config/schema/tracker.ex — Github embed has :repo (string "owner/repo"). Legacy compatibility: Linear backend (src/lib/aiur/linear/) never sets the identity — must remain valid with identity absent. Tests: src/test/aiur/github/issues_test.exs has the normalize fixtures to extend; StatusReport shapes are asserted in orchestrator tests under src/test/aiur/orchestrator/.


### BO-005

Extract the fold from src/lib/aiur/agent_list/event_intake.ex (Aiur.AgentList.EventIntake.fold/2 — pure, takes %{kind: :publish, topic, body} entries): record_latest_event -> latest_event_by_id %{topic, message, timestamp}; record_progress_sample -> progress_by_id rings via Aiur.ProgressTracker.record/3 with source ratchet (checkin always records, phase only >= head, event_intake.ex:125-126); record_phase -> phase_by_identifier (last .start wins, .end clears only if matching). Event vocabulary: topics `ticket.<id>.agent.progress[.checkin|.phase]` (body percent), `ticket.<id>.agent.phase.<brainstorm|plan|work|review>.<start|end>`; published through Aiur.Events.Publisher.publish (src/lib/aiur/events/publisher.ex, IDs + contamination filter + dedup) -> Aiur.Events.Exchange fan-out; agents emit via codex/dynamic_tool/emit_event.ex and emit_alert.ex; operator progress pings from src/lib/aiur/progress_checkin/worker.ex (topic ticket.<id>.operator.progress_request). CRITICAL transport gotcha: the current intake subscribes to Aiur.Events.DebugLog (topic "aiur:events:debug", entry shape debug_log.ex:27-34) unconditionally in Aiur.AgentList.App.subscribe/1 (app.ex:243) even though DebugLog's moduledoc calls itself debug-only — the new supervised projection must take a first-class subscription (Exchange/SubscriptionStore in src/lib/aiur/events/) or subscribe to DebugLog knowingly; do not copy the stale moduledoc assumption. Characterize the roster pruning being replaced: Aiur.AgentList.Roster.fold/2 (roster.ex:16-58) Map.take's all three maps to the visible summary set and seeds deactivated rows to 100% (seed_deactivated_progress_samples). Do NOT copy StatusReport fields: execution/waiting/backend/model live in Aiur.Orchestrator.StatusReport.snapshot/1 (status_report.ex:115-139) and running_summaries/1 (:294-364, backend via CodingAgent.backend_for, model via CodingAgent.model_for), broadcast via AgentPubSub.broadcast_running_change on AgentEvents.running_topic. Supervision entry goes in the application tree (serializes with BO-003/BO-016 as ticket says). Identity: all current keys are bare ticket-number strings parsed from topics (extract_ticket_id, event_intake.ex:52-59); repository-qualified identity does not exist yet — it arrives from BO-004/BO-017.


### BO-006

Consumer being migrated: Aiur.AgentList.App (src/lib/aiur/agent_list/app.ex, GenServer TUI pane) + Aiur.AgentList.State (state.ex — fields latest_event_by_id :105, progress_by_id :119, phase_by_identifier :125) + EventIntake + Roster + Summaries + Selection + Activation + Controls. Cutover = remove DebugLog.subscribe() + handle_info({:event_debug, entry}) -> EventIntake.fold (app.ex:221-225, 243) and consume BO-005 snapshots/PubSub instead; KEEP the :running_changed path (app.ex:158-164 Roster.fold over StatusReport running_summaries — that is the lifecycle/waiting/backend owner), warmth intake, pane activation, selection. HARD GOTCHA (documented project memory + in-code comment app.ex:295-303): RenderState.build/1 explicitly threads every state field the renderer sees — any new/renamed field must be added to render_state.ex's Map.take/put pipeline or the renderer silently never sees it. The ⏹️ completed-runner behavior to characterize: AgentEvents.state_emoji(:completed) = "⏹️" (agent_events.ex:193); rows get work_state :completed from control.status in running_summaries; completed_provenance semantics in orchestrator/state.ex:227-232; replacement-wait flow in pause_resume.ex replace_completed_issue/dispatch_completed_replacement/restore_completed_entry (:278-402) — entry keeps pid: nil, consumes no active slot (State.active_running_count excludes it; Slots.available_slots slots.ex:209-216). Selection identity: agent_list/selection.ex; sorting/summaries: agent_list/summaries.ex.


### BO-007

Pure presenter, all inputs are new-contract (BO-001/003/005) except StatusReport: consume the snapshot shape from Aiur.Orchestrator.StatusReport.snapshot_api/0 (status_report.ex:18-22 read API with 15s timeout, snapshot/1 :115-139) — per-member execution facts are running_snapshot fields (:125-188): work_state, pause_reason, waiting_reason, tracker_paused, runtime_seconds, ci_result, tokens; retry rows :191-211; idle rows :213-235. Reuse pure-presentation conventions from AiurWeb.ControlCenterPresenter (src/lib/aiur_web/control_center_presenter.ex) and AiurWeb.Presenter (presenter.ex) — pure functions over snapshots, no process/network calls. No Tarjan/SCC/topological helpers exist in the codebase; write the bounded (<=100 nodes) pure graph module fresh, deterministic ordering (sort keys explicitly, never rely on map iteration). Keep it under a new module (e.g. AiurWeb.BuildOrderPresenter per ticket contract 'BuildOrderViewModel'); LiveView (BO-012) and layout tickets consume it. Identity join: exact repository-qualified tracker identity from BO-004 — bare `identifier` strings in today's StatusReport are issue identifiers without repo qualification, so the BO-004 adapter must wrap them before this join.


### BO-009

Vendoring pattern to copy: src/lib/aiur_web/static_assets.ex — a module attribute map of path -> {content_type, body}. GOTCHA: the phoenix JS files are embedded at compile time via File.read! into module attributes; do NOT do that for an ELK-sized bundle (~1.5MB) — instead copy the aiur-logo.png pattern (StaticAssets.fetch("/aiur-logo.png") at static_assets.ex:27-35 reads at runtime from Application.app_dir(:aiur, "priv/static/...")). Put the engine + worker JS in src/priv/static/, add fetch clauses, add routes in router.ex inside the existing `scope pipe_through(:dashboard_auth)` static block (router.ex:86-94, e.g. get "/vendor/elk/elk.js"), and add controller actions in src/lib/aiur_web/controllers/static_asset_controller.ex following dashboard_css/aiur_logo. Serve pattern sets cache-control public,max-age=31536000 (static_asset_controller.ex:26-36) with NO digest — you MUST add cache busting yourself (simplest: version the URL path, e.g. /vendor/elk/<version>/elk.js, since no Phoenix static digest pipeline exists). There is NO CSP today (endpoint.ex/router.ex only use put_secure_browser_headers) — 'compatible with current CSP' means 'don't add inline eval'; if the ticket wants worker-src/CSP guarantees you must introduce the header. There is NO npm/esbuild pipeline: root package.json is dev-tooling only, published CLI is packaging/npm/aiur-cli; the 'reproducible asset pipeline' must be a documented script that produces the committed bundle. Release inclusion: priv/static ships with the OTP release automatically (aiur-logo.png proves the app_dir path works in releases). Repo limits: mix credo --strict runs on all files — exclude the vendored bundle from credo/fmt (check .credo.exs / .formatter.exs excludes).


### BO-010

Hook registration point: src/lib/aiur_web/components/layouts.ex:41-113 — Hooks object built inline in the root layout <script>, passed to new LiveView.LiveSocket("/live", ...) at line 110. Add the layout hook there (follow Hooks.AgentLogPanel lines 43-88 for mounted/beforeUpdate/updated/destroyed lifecycle and listener cleanup). LiveView JS is the vendored window.LiveView global (no imports/modules — plain function-style JS, no bundler). Server-side markup lands in a new component under src/lib/aiur_web/components/operator_control_center/ (follow fleet_table.ex: `use Phoenix.Component`, attr/1 declarations, one public function component). Attach via phx-hook with a DOM id, exactly like agent_log_modal.ex:31 (`id={"agent-log-panel-#{...}"} phx-hook="AgentLogPanel"`) — phx-hook REQUIRES an id. CSS goes in src/priv/static/dashboard.css (single 2,237-line hand-written file, served at /dashboard.css; note StaticAssets embeds it at compile time via @external_resource, so editing it requires recompile). Generation tagging: put root/generation in data-* attributes on the hook element; server assigns come from DashboardLive-style assigns. Tests: only Phoenix.LiveViewTest + floki exist today; real browser coverage depends on BO-008's harness (does not exist yet).


### BO-012

Routes: add to src/lib/aiur_web/router.ex inside the existing `scope "/", AiurWeb do pipe_through([:dashboard_auth, :browser])` block (lines 96-102): live("/build-orders", ..., :build_orders) and live("/build-orders/:root_number", ..., :build_order). MUST be placed before the catch-all scope at router.ex:135-144 (match :* "/*path" -> not_found). Two options: (a) add live_actions to AiurWeb.DashboardLive (src/lib/aiur_web/live/dashboard_live.ex, 316 lines) following the existing `:if={@live_action in [...]}` render branching (dashboard_live.ex:189-230) — this is what /decisions does; or (b) a new LiveView module `use Phoenix.LiveView, layout: {AiurWeb.Layouts, :app}` (dashboard_live.ex:6) — prefer (b) to avoid bloating render/1, but then re-render the shared chrome (Overview.topbar/readonly_banner/tabs from src/lib/aiur_web/components/operator_control_center/overview.ex) or wait for DASH-001's extracted shell (declared serialization edge). URL param handling pattern: handle_params + assign_selected_decision (dashboard_live.ex:60-65, 258-266); path helpers pattern: src/lib/aiur_web/operator_control_center/decision_path.ex. Data loading/refresh pattern to copy: PayloadLoader (src/lib/aiur_web/operator_control_center/payload_loader.ex) — subscribe in mount when connected?(socket) (dashboard_live.ex:32-35), debounced reload via handle_info + Process.send_after, per-provider health map from ControlCenterPresenter.state_payload (control_center_presenter.ex:38-53 safe_read/unavailable_fleet degradation pattern). Auth is inherited from the scope — no per-LiveView auth code. 'Existing route regression' = keep /, /decisions, /decisions/:id and /analytics untouched. Nav registration: today's nav is Overview.tabs/1 (overview.ex ~115, two hardcoded patch links) — if DASH-001's registry hasn't landed, add a third link there; do not invent client-side tabs.


### BO-013

There are NO existing focus/dialog helpers to refresh — build them. Existing interaction state precedent: fleet filter toggling is a server-side event + pure policy module (dashboard_live.ex:100-104 toggle-fleet-filter -> FleetFilters.toggle, src/lib/aiur_web/components/operator_control_center/fleet_filters.ex — pure MapSet reducer, separately unit-tested). Copy that shape for selection/zoom reducers: pure Elixir module + thin handle_event, or pure JS reducer inside the layouts.ex hook. Theme/reduced-motion: theme is data-theme on <html> toggled by Hooks.ThemeToggle (layouts.ex:90-108) persisting to localStorage 'aiur-theme'; dashboard.css keys off [data-theme]. There is no reduced-motion or forced-colors handling anywhere yet — add @media (prefers-reduced-motion)/(forced-colors) sections to src/priv/static/dashboard.css. Design tokens live as CSS custom properties at the top of dashboard.css. Keyboard/ARIA: current code uses aria-pressed on toggle buttons (overview.ex fleet_overview) and sr-only headings (fleet_table.ex:23) — follow those conventions. Browser/touch tests require BO-008's harness (nothing exists; LiveViewTest cannot exercise pan/zoom).


### BO-014

All observer/coalescing work lives in the layouts.ex hook (or the BO-010 hook file if BO-010 extracted it — refresh at pickup); the existing debounce precedent to imitate server-side is PayloadLoader.schedule (payload_loader.ex:28-38: 50ms debounce + 400ms min interval + scheduled? flag on assigns — a coalescing pattern reviewers will recognize). Cleanup discipline precedent: every existing hook removes its listeners in destroyed (layouts.ex:71-74, 105-107) — extend to ResizeObserver/worker termination. Responsive CSS: src/priv/static/dashboard.css is the single stylesheet; it already has narrow-width table->card treatment (data-label attributes on <td> in fleet_table.ex:48-71 pair with CSS that renders them as labels at narrow widths) — keep Build Order rules namespaced under a route-specific class per the ticket's 'do not edit shared OCC/Units/Commands CSS' rule. Performance budgets/fixtures belong to BO-008's harness (does not exist on main; only ExUnit+LiveViewTest). No perf measurement infra exists in the web layer today.


### BO-016

Two pieces: (a) detail read adapter — REUSE Aiur.GitHub.Issues.fetch_issue_raw/2 (issues.ex:26 — GET /repos/o/r/issues/N returning the full raw map incl. body, node_id, state_reason, html_url, timestamps) as the transport call, but wrap it in a new module (e.g. Aiur.BuildOrder.TicketDetail) that first validates the BO-004 identity (reject before I/O: compare requested {owner,repo} against Transport.parse_repo/0 result and match node_id after fetch) and then sanitizes/bounds title+body before returning a snapshot struct; error taxonomy via Aiur.GitHub.Errors.classify_error/1 (errors.ex — already yields :auth/:rate_limited/:timeout/:http classes; 404 arrives as {:github,:http,%{status: 404}}, map it to :not_found). Safe URL: keep only gh_issue["html_url"] after prefix-validating against https://github.com/<owner>/<repo>/. (b) supervised cache GenServer — same recipe as BO-003 (add to src/lib/aiur.ex child_specs list — this is the shared seam behind 'serializes with BO-003/BO-005/BO-019'); coalesce concurrent demand by keying in-flight Task refs by identity in state; monotonic generation counter per identity; bounded retention via map size + LRU eviction. Redaction reference: src/lib/aiur/secret_redactor.ex exists for scrubbing text. Publish seam for BO-018: a fetch/subscribe API + Phoenix.PubSub topic 'build_order:detail:<node_id>' on Aiur.PubSub (copy AgentPubSub wrapper style). Tests: request_fun stubs (canned 200/404/403 responses) — assert the mock is NEVER invoked for a mismatched-repo identity.


### BO-017

The event system to extend: Aiur.Events.Publisher.publish/3 (src/lib/aiur/events/publisher.ex:89 — every event is a bare map merged with %{id:, topic:}; opts carry :issue_number/:actor/:dedup_key) fanning out via Aiur.Events.Exchange.publish/2 (events/exchange.ex:93 — ETS pattern match, send {:event, event}); topic grammar 'ticket.<id>.<surface>.<verb>' matched by Aiur.Events.Topic (* / # wildcards). PRODUCER INVENTORY (verified call sites to migrate/wrap): src/lib/aiur/agent_runner/tool_executor.ex:214 (agent emit_event — publishes ticket.<id>.agent.* incl. agent.progress %{percent: 0..100}), src/lib/aiur/alerts.ex:258 (emit_alert), src/lib/aiur/progress_checkin/worker.ex:101 (ticket.<id>.operator.progress_request + agent.progress.checkin), src/lib/aiur/orchestrator/ci_lifecycle.ex:268 (ci.passed/failed), src/lib/aiur/orchestrator/command_scan.ex:163, src/lib/aiur/events/github_firehose.ex (pr.opened/pr.merged/issue.commented), src/lib/aiur/events/ls_remote_ticker.ex (branch.push), src/lib/aiur/decision_store.ex:1355+2557 (publish_persisted — durable path, do NOT break its persist-before-notify contract; Publisher rejects decision.* topics on the plain path, publisher.ex:141). Envelope approach: add optional envelope fields to the payload map at the Publisher boundary (one normalizer) rather than editing every producer — Publisher.publish/3 is the single funnel; attach BO-004 identity from a new opt (e.g. :identity) and record occurred_at (source) vs observed_at (DateTime.utc_now at publish). Subscribers that must stay compatible: Aiur.Events.SubscriptionStore (per-agent queue), Aiur.AgentList.EventIntake (src/lib/aiur/agent_list/event_intake.ex:81 folds agent.progress topics), Aiur.IssueLog.record_event (issue_log.ex:307 — called from Publisher via extract_ticket_id). Consumed-side markers written by Publisher into IssueLog depend on event maps having :id/:topic/:message keys (issue_log.ex:358-388) — keep those keys stable. Legacy classification: events lacking the identity opt remain publishable but flagged unattributed; never derive repo from the topic string (extract_ticket_id at publisher.ex:167 is routing-only).


### BO-018

Presenter pattern to copy: src/lib/aiur_web/operator_control_center/decision_presenter.ex (pure rows/1 over store structs, no I/O) plus ControlCenterPresenter's safe_read/provider_health degradation (control_center_presenter.ex:223-243) for distinct missing/stale/unavailable states. Component pattern: src/lib/aiur_web/components/operator_control_center/decision_detail.ex — attr-declared function component, private detail_block/1 sub-component (lines 119-129), metadata <dl> list (lines 75-86), timeline <ol> (lines 98-106); this is the closest existing 'ticket context' rendering. Dialog markup: copy agent_log_modal.ex structure (modal-backdrop/modal-panel divs, role=dialog aria-modal aria-labelledby, close button wired to a phx-click) and the AgentLogPanel scroll hook for the bounded Logs timeline — but AgentLogModal has NO focus trap/Escape/restore; BO-018 must add a new JS hook for that (register in layouts.ex Hooks). CTA link policy: copy trusted_url/1 from decision_detail.ex:151-158 (http/https+host check) and the link-pill markup with target=_blank rel="noopener noreferrer" (decision_detail.ex:92); consider extracting to one shared module since it's currently triplicated (fleet_table.ex:173, decision_detail.ex:151, recent_outcomes.ex). What NOT to reuse: AgentLogModal.build/refresh do live filesystem reads via Aiur.AgentLog.read_workspace/1 (agent_log_modal.ex:76-117) — BO-018's no-I/O-at-render contract explicitly forbids this; inputs must be BO-016/BO-019 snapshots (backend tickets, not yet built). State handling: modal open/close as a DashboardLive assign (:agent_log_modal, dashboard_live.ex:43,112-121) is the existing pattern; the URL-backed alternative is /decisions/:decision_id + assign_selected_decision (dashboard_live.ex:258-266).


### BO-019

Depends on BO-005 (activity fold — not yet built); this ticket adds a provider GenServer, e.g. Aiur.BuildOrder.TicketHistoryProvider, registered in src/lib/aiur.ex child_specs (shared seam — serialize with BO-003/BO-016). SUPPORTED SOURCES that exist today: (1) Aiur.IssueLog.event_history/2 (src/lib/aiur/issue_log.ex:108 — typed query: opts :since_id/:kinds([:emit,:emit_alert,:consumed,:self])/:limit, returns maps %{kind, id, topic, ts, summary, source, author_trusted?}; it parses the daemon's own structured [event:*] marker lines from <logs-root>/log/<repo>.<issue>.log — this IS the supported IssueLog seam, not 'raw log parsing'; the raw-log prohibition targets agent.md/NDJSON/terminal panes/model transcripts); (2) Aiur.IssueLog.history/2 (:62 — in-memory ring of last 100 transcript/alert events, [] when no writer — map [] to :missing_source not :known_empty); (3) live events by subscribing Aiur.Events.Exchange.subscribe("ticket.<id>.#") (exchange.ex:71) — delivery is send(self(), {:event, event}); (4) current progress: agents publish ticket.<id>.agent.progress %{percent:} — projection helpers already exist in Aiur.ProgressTracker (record/3, estimate/2, @stale_after_ms 300_000 — reuse its staleness semantics); (5) decision timeline if allowlisted: Aiur.DecisionStore.recent_audit_history/2 (decision_store.ex:227 — capped @recent_audit_limit 50, returns %{records, contexts, revisions}). NAMING GAP: the ticket says 'Aiur.EventBus' — no such module; read it as Aiur.Events.* . Bounds: default 50 / hard 100 mirrors both @recent_audit_limit 50 and IssueLog @history_limit 100 — reuse those numbers. Snapshot shape: key by BO-004 identity, include source health enum :available/:known_empty/:missing_source/:stale/:unavailable/:restart_unknown; publish on Phoenix.PubSub 'build_order:history:<node_id>' (AgentPubSub wrapper style). Restart truth: IssueLog files DO survive daemon restart (~/.aiur/logs persist, memory note PR #256) but the in-memory ring and Exchange subscriptions do not — document exactly that.


### DASH-001

Extract from: src/lib/aiur_web/live/dashboard_live.ex render/1 (lines 176-240 — topbar/banners/tabs are already delegated to components, so the real work is a route registry replacing Overview.tabs/1) and src/lib/aiur_web/components/operator_control_center/overview.ex topbar/1 (lines 22-42: brand, Live/Offline badges, ITS/Agent kind badges from Aiur.Config.tracker_kind/agent_kind via dashboard_live.ex:288-289, clock, ThemeToggle button) and tabs/1 (~line 115: two <.link patch> entries with counts). Route facts for the registry: live routes /, /decisions, /decisions/:id use push_patch/<.link patch> (same LiveView); /analytics is a controller document (TelemetryDashboardController) — must be a plain <a href>, NOT patch/navigate; its availability signal already exists in the payload as @payload.analytics %{available?, path, message} (control_center_presenter.ex:215-221, presenter.ex analytics_payload — available? is false until a telemetry file exists, use it for the 'visibly unavailable' state). Active-route input: @live_action (assigns are :index/:decisions/:decision). Layouts: root layout src/lib/aiur_web/components/layouts.ex (holds ALL inline JS+LiveSocket — touch carefully), app layout is just <main class="app-shell"> (layouts.ex:128-135). CSS: src/priv/static/dashboard.css (recompile needed after edits — compile-time embedded via @external_resource in static_assets.ex). Read-only/writable: @writable from Endpoint.config(:dashboard_writable) (dashboard_live.ex:275) + Overview.readonly_banner. Preserve router.ex URLs exactly; new nav must not break the LiveViewTest suite in src/test/aiur_web/live/dashboard_live_test.exs and router_auth_test.exs. #1034 Executor terminology is already on main (e.g. 'Executor Control Center' in layouts.ex:19, dashboard_live.ex:3).


### DASH-002

Run identity: Aiur.Boot.run_id/0 (boot.ex:92-98, persistent_term, one per BEAM run; remark/0 :50-58 mints a new one — use to test daemon-restart-vs-projection-restart); Boot.started_at/0 (:105-111). Lifecycle observations: StatusReport.snapshot_api/0 (running/retrying/idle) — queue/retry entries from state.retry_attempts (retry_snapshot status_report.ex:191-211), idle from state.last_polled_issues (:213-235), running control status via [:control, :status] and paused_reason; terminal detection via Aiur.Orchestrator.DispatchPolicy.terminal_state_set/0. Persistence reuse targets (the 'daemon-private state root, atomic/checksummed file patterns'): Aiur.Fs.atomic_write(path, contents, fsync: true, mode: 0o600) (src/lib/aiur/fs.ex; example use recent_merge_store.ex:346), state root Config.Paths.decision_state_dir() (recent_merge_store.ex:89-92 — note the helper is decision-named; add a sibling path helper for the membership journal rather than nesting under decisions), append-then-atomic-compaction pattern in recent_merge_store.ex (moduledoc), JSONL helpers src/lib/aiur/jsonl.ex and json_store.ex, content_hash convention in recent_merge.ex. Canonical Issue struct: src/lib/aiur/issue.ex (id, identifier, state, paused?/1). PubSub health convention: AiurWeb.ObservabilityPubSub.broadcast_update (src/lib/aiur_web/observability_pubsub.ex) and AgentPubSub topics (agent_pubsub.ex). No membership journal or projection exists today — new supervised child in the application tree (serialize per ticket's long peer list).


### DASH-003

Extend/replace: src/lib/aiur_web/components/operator_control_center/fleet_table.ex (183 lines — table markup, data-label responsive cells, row action buttons pattern at lines 71-101: decision link, agent-log button, tracker link each with onclick="event.stopPropagation()"), fleet_filters.ex (85 lines — the pure filter policy DASH-016 supersedes), overview.ex fleet_overview/1 (the stat-chip filter buttons with aria-pressed). Remove/replace the clickable <tr> (fleet_table.ex:42-47) with explicit named buttons per the ticket. Wire into DashboardLive: today fleet renders when @live_action not in [:decisions,:decision] (dashboard_live.ex:206-213); a /units route comes from DASH-001's registry. URL-backed filters: current fleet filters are socket-state only (toggle-fleet-filter event, NOT in URL — dashboard_live.ex:100-104); the decisions filter IS URL-backed via push_patch + handle_params (dashboard_live.ex:93-98, 60-65, decision_path.ex) — copy the decisions mechanism, and consume DASH-016's URL codec rather than writing param parsing here. Data: keep the PayloadLoader/ControlCenterCache/PubSub reload path (payload_loader.ex) and feed rows from DASH-016's UnitsRow API instead of FleetFilters.rows/1. Row fields available today per entry: presenter.ex:258-291 (issue_identifier, title, url, work_state, waiting_reason, open_decision_count, ci, tokens, runtime_seconds, workspace_path — DO NOT render workspace_path in the new rows). Temporary compat: keep show-agent-log handler (dashboard_live.ex:112-117) until DASH-027 lands, per ticket. Tests: extend src/test/aiur_web/live/dashboard_live_test.exs and operator_control_center_components_test.exs.


### DASH-004

Existing control seams to extend (do NOT build a parallel RPC): Aiur.AgentChat.pause/1 / resume/1 (agent_chat.ex:71-80 — pause also arms Aiur.PauseContainment; preserve that) -> Aiur.Orchestrator.pause_agent/resume_agent -> Aiur.Orchestrator.PauseResume (pause_resume.ex:22-48, control_api_call = GenServer.call 5s). The optimistic behavior the ticket must fix/characterize: pause_running_or_inactive (pause_resume.ex:199-212) flips control.status :paused immediately after queueing the control message; resume sync-flips :working in send_resume_control_message (:500-549, 'Sync-flip happens here' comment) before any worker confirmation. Correlation infra ALREADY half-exists: OperatorMessages.send_running_control_message/3 (orchestrator/operator_messages.ex:606-617) mints request_id = :erlang.unique_integer([:positive]) and sends {:pause_agent, request_id} / {:resume_agent, request_id} to the worker pid; worker handles them in agent_runner.ex:183-188; worker reports back {:worker_control_state, issue_id, :paused, pause_payload} where pause_payload can carry :request_id (message_handler.ex:180; worker_pause_reason checks it, pause_resume.ex:649-657) — BUT the :working/:completed ack is a 3-tuple with no request_id (message_handler.ex:170), and handle_worker_control_state (pause_resume.ex:224-248) applies status without matching request_id to a pending request. Extend these envelopes with generation + request correlation. completed_provenance transitions to characterize at pickup: State.completed_provenance? (orchestrator/state.ex:227-232), replace_completed_issue/restore_completed_entry/restart_completed_provenance_issue (pause_resume.ex:278-431), pause_completed_issue_for_label_override (:131-147). Capability discovery seam: OM.issue_control_capabilities (used at status_report.ex:126) and Orchestrator.control_capabilities via AgentChat.capabilities/1. Observability: AgentPubSub.broadcast_alert (agent_pubsub.ex:109+), lifecycle telemetry Aiur.RunTelemetry.Lifecycle.record with :agent_pause/:agent_resume (pause_resume.ex:551-574). Pause attribution fields to preserve: paused_reason values seen in code — :operator_pause, :label_override, :max_agent_duration, :input_required, :worker_pause_unknown, :pause_containment (grep pause_resume.ex); auto-resume of spurious worker pauses at :630-647.


### DASH-005

Single LiveView today: AiurWeb.DashboardLive (src/lib/aiur_web/live/dashboard_live.ex) with actions :index/:decisions/:decision; the Units page it depends on (DASH-003) does not exist yet — this ticket lands on DASH-003's seam. Auth/write gates to reuse: :dashboard_auth pipeline + supervisor_auth.ex (Basic Auth); writable mode = :require_writable pipeline (router.ex:110-119) and @writable assign already threaded into components (dashboard_live.ex:231-236 AgentLogModal receives writable/drafts/chat_errors — the existing control/error copy pattern the ticket says to reuse minus its optimistic behavior). Existing browser-write precedent: POST /api/v1/:issue_identifier/messages via ObservabilityApiController :send_message (router.ex:117). Control lifecycle facts come from DASH-004; row eligibility inputs come from StatusReport running_snapshot (work_state, pause_reason, control capabilities map at status_report.ex:126,182). Serializes on shared DashboardLive/CSS with DASH-007/015/021/022/027/028/031/034 — the whole dashboard is one LiveView module today, so these serializations are real file-level conflicts.


### DASH-007

Files to edit (vocabulary + filter regrouping, no domain rename): src/lib/aiur_web/components/operator_control_center/decision_inbox.ex (@filter_specs list at lines 8-16 currently [:all,:open,:blocking,:undelivered,:supervisor,:resolved,:superseded] -> primary Open/Blocking/Resolved/All; keep secondary states reachable), decision_card.ex, decision_detail.ex (metadata <dl>, options list, links, timeline), decision_action.ex + decision_revision_action.ex (write flows with confirmation/version handling — preserve untouched semantics), decision_latency.ex, history.ex, lifecycle_components.ex (lifecycle_stepper), and the filter atoms in dashboard_live.ex:25 (@decision_filters) + normalize_filter (dashboard_live.ex:268-274). URL/filter plumbing: filter-decisions event -> push_patch -> DecisionPath.inbox/detail (src/lib/aiur_web/operator_control_center/decision_path.ex); direct detail route /decisions/:decision_id already exists — preserve it. Presenter: src/lib/aiur_web/operator_control_center/decision_presenter.ex rows/1 is where card-facing fields are shaped; supervisor_basis.confidence flows from Aiur.DecisionStore/decision_api — render as-is on 0..100. Banner counts: Overview.decisions_banner + fleet tabs count use length(@payload.decisions) which is DecisionStore.recent_decisions(50, ...) (control_center_presenter.ex:33) — i.e., today's counts ARE the bounded newest-50 window; DASH-006's canonical counts must replace them per the ticket. History surface: RecentOutcomes + History components render on :index (dashboard_live.ex:215-230) with @decision_history_limit 50 (control_center_presenter.ex:11). Write handlers to leave intact: DecisionEvents module (src/lib/aiur_web/operator_control_center/decision_events.ex) dispatched via `event in @decision_events` (dashboard_live.ex:106-110) behind handle_writable_event.


### DASH-008

Greenfield schema+registry; suggest src/lib/aiur/usage/envelope.ex and src/lib/aiur/usage/token_relationship_registry.ex. Read FIRST: src/lib/aiur/token_usage.ex (canonicalize/1 is the transient contract you must keep alive as the compatibility projection — it returns only %{input_tokens, output_tokens, total_tokens} with ||0 zero-fill, which your envelope explicitly must NOT do) and src/lib/aiur/orchestrator/token_accounting/payloads.ex (the real raw shapes an envelope must represent: Codex absolute counters at params.msg.payload.info.total_token_usage / params.tokenUsage.total, turn/completed usage maps, rate-limit maps keyed limit_id+primary/secondary/credits). Counter semantics evidence: TokenAccounting.compute_token_delta (token_accounting.ex:219-234) does monotonic max-of-absolute with lower-value-means-zero — that is the transient delta logic DASH-009 will replace; your measurement_kind/counter_scope/counter_epoch fields must be able to describe both the Codex thread-absolute stream and turn/completed deltas. run_id: Aiur.Boot.run_id/0 (boot.ex, persistent_term). Ticket identity today is Issue.identifier (string) passed through MessageHandler; attempt_id exists only in RunTelemetry lifecycle_opts — BO-017 is supposed to formalize this, so define typed-but-optional identity fields. Exact money: Decimal 2.3.0 is ALREADY available transitively via ecto (mix.lock) — use it; the gotcha is Jason decodes JSON numbers to float at the port, so your exact-decimal helper must accept a raw string or integer minor units, and DASH-029 must re-decode at the transport (claude cost_usd today goes through put_if_number, i.e., float — claude/coding_agent.ex:393). Validation style precedent: decision.ex/decision_event.ex structs with explicit validators. Tests: property tests live under src/test/aiur/.


### DASH-009

Copy Aiur.DecisionLog (src/lib/aiur/decision_log.ex) almost verbatim for the append side: raw-fd append, newline-terminated JSON objects, fsync-before-ack, Fs.sync_filesystem on first directory creation, torn-tail truncation on replay, caller-supplied validator (NOT bare JSON-decode success), symlink rejection. Checkpoints: Aiur.JsonStore (src/lib/aiur/json_store.ex, write-to-.tmp+fsync+rename; consumers to imitate: Events.IdGenerator, Events.SubscriptionStore). NDJSON streaming: Aiur.Jsonl.stream/1. State-root precedent: DecisionStore uses decisions.ndjson under the daemon state root (decision_store.ex:56 @ndjson_filename); ModelAvailability instead writes beside the workflow file — follow the DecisionStore/private-state pattern, not ModelAvailability's. Supervision: add the writer GenServer to the application supervision tree (lib/aiur/application.ex) — this file is the declared serialization hotspot with BO-003/005/016/019, DASH-002/012/019, so expect rebase conflicts there and nowhere else. The delta logic you are making durable currently lives in TokenAccounting.extract_token_delta/compute_token_delta (token_accounting.ex:182-234): monotonic per-running-entry agent_last_reported_*_tokens; do NOT delete TokenAccounting — the ticket requires keeping it as a compatibility consumer. Checkpoint key fields come straight off the DASH-008 envelope; store generation is a new namespace (do not reuse Boot.run_id). Test precedents: decision_log tests cover crash/torn-tail/corruption patterns to copy; SubscriptionStore tests show the :log_file-per-test isolation gotcha.


### DASH-011

Pure greenfield library — zero pricing/cost/currency code exists anywhere in the repo today (verified: only cost_usd passthrough in claude/coding_agent.ex:393). Suggest src/lib/aiur/usage/pricing.ex + src/lib/aiur/usage/price_table.ex (checked-in versioned fixture data). Use Decimal (available via ecto dep, mix.lock decimal 2.3.0) or integer micros — never float; JSON price fixtures must be decoded with string values, not JSON numbers, to keep exactness. No file I/O, no GenServer, no provider calls: a pure module keyed on the DASH-008 registry structs plus a price-table map — this is the most cheap-model-friendly ticket in the family IF DASH-008's registry shape is pinned first; do not pick up before DASH-008 merges. Price table keys are exact resolved_model strings as they appear in provider events (nothing in the repo normalizes model names — do not invent normalization). The tier-join key output is just a 3-tuple (provider, backend, provider_account_generation) passed through from the envelope; emit it only when generation is known, nil otherwise. Seed price fixtures at pickup from docs.anthropic.com/en/docs/about-claude/pricing and OpenAI pricing with recorded effective dates; validate non-overlap of effective-date intervals at compile/config-load time.


### DASH-014

Run identity/start: Aiur.Boot.run_id/0 AND Boot.started_at/0 both already exist (boot.ex:92-111, wall clock + monotonic mark from the same utc_now at :33-37) — the ticket's contingency 'add run start to the existing run owner' is unnecessary; consume Boot directly with an injected clock for tests. The cumulative-vs-wall trap it warns about is real: state.agent_totals (status_report.ex:131, maintained in orchestrator/token_accounting.ex) and per-entry runtime_seconds = State.running_seconds(started_at_of_agent, now) are per-agent runtimes; never sum them. Complexity weights: CodingAgent.complexity_level(issue) (src/lib/aiur/coding_agent.ex; used at status_report.ex issue_complexity) parses complexity:N labels 1..5 — reuse it for the weight policy. Counts/predicates come from DASH-016's UnitsRow catalog (new, not in main); membership from DASH-002. PubSub/health/generation conventions: ObservabilityPubSub.broadcast_update + AgentPubSub patterns. Keep the projection pure + supervised like BO-005; exact math with rationals until display.


### DASH-016

This is a headless read-model: put new modules under src/lib/aiur_web/ or src/lib/aiur/ as pure modules with NO Phoenix imports — model on FleetFilters (src/lib/aiur_web/components/operator_control_center/fleet_filters.ex: pure functions rows/visible_rows/counts/state over plain maps, MapSet filter state, separately tested in operator_control_center_components_test.exs). Inputs to join: (a) fleet snapshot rows from AiurWeb.Presenter running/retry/idle entry payloads (src/lib/aiur_web/presenter.ex:258-330 — field names for the row struct: issue_id, issue_identifier, state, title, url, work_state, waiting_reason, pause_reason, tracker_paused, open_decision_count, runtime_seconds, started_at, last_event_at, stale_for_seconds, ci, tokens), sourced from Aiur.Orchestrator.snapshot/2 -> Aiur.Orchestrator.StatusReport (src/lib/aiur/orchestrator/status_report.ex); (b) DASH-002 membership and BO-005 activity (do not exist yet — hard deps); (c) Decision counts already joined per-row as open_decision_count. Existing waiting_reason vocabulary for the condition truth table: FleetFilters @blocked_reasons [:waiting_for_human,:waiting_for_supervisor,:waiting_for_dependency], @stuck_reasons [:backing_off,:unresponsive], plus :awaiting_dispatch and :active seen in fleet_table.ex/presenter defaults; work_state values :working/:paused/:completed. The replacement-boundary case (work_state :completed + waiting_reason :awaiting_dispatch) is real vocabulary on main. Sanitization rule with a concrete target: presenter rows currently include workspace_path (presenter.ex:267,301) — UnitsRow must NOT carry it. Trusted URL: copy trusted_url/1 (fleet_table.ex:173-180). Keep row-build, predicates, counts, URL codec as separate modules (repo module-size limits enforced by credo --strict; run make lint).


### DASH-022

Pure presentation over DASH-014's snapshot; the mount/reload pattern to copy is DashboardLive.reload_payload/2 -> AiurWeb.OperatorControlCenter.PayloadLoader.load(mode) (dashboard_live.ex:242-246, src/lib/aiur_web/operator_control_center/payload_loader.ex) plus ObservabilityPubSub subscription for coalesced reloads. Lands on DASH-003's Units page (does not exist yet). Component convention: Phoenix.Component modules under src/lib/aiur_web/components/operator_control_center/ (e.g. overview.ex is the current stat-card row — closest visual precedent for a summary strip; recent_outcomes.ex shows the chip/health-state copy pattern). Keep formatting in a presenter module (control_center_presenter.ex style), not in handle_event. ARIA progress: omit aria-valuenow when DASH-014 reports unknown-progress weight > 0 (the snapshot carries coverage/unknown fields per DASH-014 contract). Shares the summary container/CSS with DASH-015 — serialize, don't co-edit.


### DASH-027

Replace-target reference (what NOT to carry over): src/lib/aiur_web/components/operator_control_center/agent_log_modal.ex — path display at line 29, filesystem reads in build/refresh via Aiur.AgentLog.read_workspace/1 (lines 76-117, agent_log.ex:42), raw-JSON fallback (agent_log.ex:251-262, 387), composer/Pause controls (lines 38-52). Trigger seam: replace the clickable <tr> (fleet_table.ex:45-46) trigger with DASH-003's explicit named action; the existing explicit button precedent is fleet_table.ex:81-90 ('Read agent conversation' button firing show-agent-log). Modal state lifecycle to copy: DashboardLive assigns :agent_log_modal (dashboard_live.ex:43), open/close handlers (112-121), and refresh-on-payload-reload (reload_payload/2 line 248 calls AgentLogModal.refresh — your drawer must instead pin generation and go stale/closed, per ticket). Scroll pinning + jump-to-latest: Hooks.AgentLogPanel in layouts.ex:43-88 already implements at-bottom detection, wasAtBottom preserve-position on update, and a Live button that jumps to bottom + aria-pressed — this is exactly the ticket's auto-follow/jump-to-latest behavior; extend it, don't rewrite. Message row markup: agent_log_modal.ex:31-36 (log-message role classes via log_message_class/1, header title+timestamp, body). CSS: modal-backdrop/modal-panel/chat-log-panel classes in src/priv/static/dashboard.css. Focus trap/Escape/focus-return do NOT exist — new JS hook needed (register in layouts.ex Hooks). Data source must be DASH-026 snapshots (not yet built — hard dep); until then there is no non-filesystem transcript provider on main. Read-only banner precedent: Overview.readonly_banner + agent_log_modal.ex:54 read-only note.


### DASH-028

Backend is DONE — call it, don't build it. Read: Aiur.Orchestrator.Slots.max_concurrent_agents/0 -> %{active, paused, configured, max, session_override?, draining?} | :unavailable (slots.ex:9-21, 191-204). Mutate: Slots.adjust_max_concurrent_agents/1 (delta; clamps to min 1 at slots.ex:49) and Slots.set_max_concurrent_agents/1 (guard next > 0) — both return {:ok, status_map} | {:error, :timeout | :unavailable} (control_api_call slots.ex:68-77); also aliased as Aiur.Orchestrator.adjust_/set_max_concurrent_agents (orchestrator.ex:350-364). Render the RETURNED map as the authoritative post-state. draining? is literally active > max (slots.ex:202) — lowering never kills workers (dispatch admission just shrinks via available_slots, slots.ex:209-216). TUI precedent to mirror: Aiur.AgentList.Controls.adjust_max_concurrent_agents (agent_list/controls.ex:29-32). Two implementation facts: (1) no capacity fact reaches today's web payload (zero Slots refs under src/lib/aiur_web/) — add the Slots read into the Units payload/assign path; live refresh already works because apply_session_max_concurrent_agents calls StatusReport.notify_dashboard (slots.ex:64) which fires ObservabilityPubSub.broadcast_update (status_report.ex:63-77). (2) the map has `configured` + `session_override?` but NO `source` field — derive source labels from those two or show unknown; do not invent a revision field (ticket already forbids this). Write gating: :require_writable pipeline + @writable assign as in DASH-005.


### DASH-029

This is the family's main brownfield ticket. Extend: (1) src/lib/aiur/agent_runner/message_handler.ex:28-36 — the per-message closure (normalize_event -> observe_lifecycle -> observe_rate_limits -> AgentEventLog.write -> broadcasts -> send_codex_update at :142) is backend-agnostic and unconditionally executed, so emitting envelopes here automatically satisfies 'normal-run emission regardless of browser/TUI/debug'; note RunTelemetry.Lifecycle (debug-gated, lifecycle.ex:53-55) is explicitly NOT the vehicle. (2) src/lib/aiur/codex/event_normalizer.ex — absolute_token_usage/1 lists the exact Codex cumulative paths (params.msg.payload.info.total_token_usage etc.) and turn_completed_usage/1 the turn/completed shape; today these are collapsed through TokenUsage.canonicalize which DROPS cached_input/reasoning fields — your adapter must capture them from the raw payload before canonicalize destroys them. (3) src/lib/aiur/claude/coding_agent.ex:365-396 — normalize_event + metadata_from_message; cost_usd currently decoded via put_if_number (binary float from Jason) — to satisfy the exact-decimal invariant you must extract cost from the raw JSON string at the port boundary (codex/app_server_port.ex / claude port read path) or decode with Jason floats-as-decimals, NOT from the already-decoded map. Compatibility: keep the {:codex_worker_update, issue_id, message} send (message_handler.ex:142) untouched so TokenAccounting/StatusReport keep working. Attribution inputs available at the closure: Issue struct, backend string, workspace, turn_id, lifecycle_opts attempt_id. Serializes with BO-005 on exactly these files. No fixture corpus exists in the repo for either provider's usage events — capture sanitized fixtures at pickup as the ticket instructs.


### DASH-030

Pure projection, greenfield, over two contracts that must be merged first (DASH-024 aggregate behavior, DASH-011 pricing resolver) — do not pick up early. No grouped-query precedent exists; nearest patterns: AiurWeb.ObservabilityPubSub (src/lib/aiur_web/observability_pubsub.ex) broadcasts a bare :observability_updated atom with NO payload (LiveView refetches) — for grouped-usage change notifications follow Aiur.AgentPubSub (src/lib/aiur/agent_pubsub.ex) which does topic-per-identifier with payloads, and put snapshot state behind a GenServer/ETS the way ControlCenterCache (src/lib/aiur_web/control_center_cache.ex) fronts presenter data. Ticket identity: today tickets are Issue.identifier strings; the repository-qualified typed identity comes from BO-004/BO-017 (outside this family) — refresh that shape at pickup rather than assuming. Scope input is caller-supplied only: the two callers are Units 'this_run' (run_id from Aiur.Boot.run_id/0) and DASH-023's explicit member set; reject anything else. Arithmetic: Decimal (in deps via ecto). Cache keys must embed DASH-024 aggregate generation + DASH-011 price/relationship revisions; the stale-rejection pattern the memory notes call 'generation-keyed' has precedent in how ControlCenterCache invalidates on PubSub update.


### DASH-031

Composition target: src/lib/aiur_web/live/dashboard_live.ex — today the ONLY LiveView, serving actions :index/:decisions/:decision (router.ex:99-101); the /units page it renders into does not exist until DASH-003 lands. Existing presenter shapes to study: src/lib/aiur_web/presenter.ex:34 passes snapshot.agent_totals and control_center_presenter.ex:253 defaults %{input_tokens: 0, output_tokens: 0, total_tokens: 0, seconds_running: 0} — that zero-default pattern is exactly what this ticket forbids for money (unknown != $0.00). Auth: all reads flow through DASH-021's facade, which wraps the env-optional dashboard_basic_auth (router.ex:147-159); the denied state must be value-free at the assign level, not CSS. Live updates: subscribe via the protected channel DASH-021 defines, NOT the generic ObservabilityPubSub topic. Serialization warning: shares DashboardLive/CSS with DASH-005/007/015/022/027/028/032 — the single biggest conflict surface in the pack; coordinate merge order. Dependency red flag to surface at pickup: this ticket hard-depends on 8 tickets including BOTH human-gated Claude paths (DASH-010 via GATE-CLAUDE-OTEL-PROTOCOL-AUTHORITY, DASH-013 via GATE-CLAUDE-METER-PROTOCOL-AUTHORITY); if those gates stall, consider asking the operator for an interim codex-only acceptance rather than blocking indefinitely. All formatted money via exact decimal strings from DASH-030; use <time datetime=> machine-readable timestamps; a11y gates (320px/200% zoom/44px) match DASH-003's shell contract.


### DASH-032

Compose exactly these shipped modules: Aiur.RecentMergeStore.snapshot/0 -> %{merges: [RecentMerge.t()], health: :writable | tuple, reconciliation: %{partial?, pages_fetched,...}} (recent_merge_store.ex:46-48; never read the audit file — the store owns durability via Fs.atomic_write at :346 under Config.Paths.decision_state_dir()). RecentMerge fields (recent_merge.ex:22-44): repository, number, title (<=200), summary (<=500), url (validated), head_ref, merge_commit_sha, ticket_id (ONLY from TicketBranch.ticket_id(head_ref), :120), merged_at, backfilled?, live_observed?, observed_run_id (nil unless live-observed, :364-370), first/last_observed_at, content_hash. Locator: Aiur.TicketBranch.ticket_id/1 regex \Aaiur\/([1-9]\d*)(?:-slug)?\z (ticket_branch.ex:25,64-72) returns the bare number string — treat as locator into DASH-002 membership, resolve to BO-004 identity. Run window: DASH-014/Boot — Boot.started_at/0 (wall) + run_id/0; no run-end concept exists (active-run-only window per ticket: started_at <= merged_at <= observed_at). Repository normalizer + trusted-URL validation already live in recent_merge.ex (normalize_repository/normalize_url) — reuse, don't re-derive. Keep the projection a pure module over the three snapshots; deterministic sort by (merged_at, id); id format is "repo#number" (recent_merge.ex:111).


### DASH-034

The current Recent region to extend/relocate: AiurWeb.DashboardLive render, `section.recent-card` shown only for @live_action == :index (dashboard_live.ex:215-230), composed of (a) AiurWeb.OperatorControlCenter.RecentOutcomes.recent_outcomes/1 (src/lib/aiur_web/components/operator_control_center/recent_outcomes.ex — heading 'Recent repository merges'; chips Merged / ticket_id / 'No ticket attribution' / 'Observed live' / 'Observer run <12-char run_id>' / 'Backfilled'; partial-reconciliation + page-cap chips :59-65; trusted_url/1 scheme+host whitelist :75-79; analytics link only when %{available?: true, path: "/analytics"} :67-68) and (b) History.history/1 (Decision history — this is what DASH-007 must keep reachable through Commands before this ticket touches the region). Payload plumbing: ControlCenterPresenter builds recent_outcomes / recent_outcomes_health / recent_outcomes_reconciliation / analytics (control_center_presenter.ex:42-102) via PayloadLoader. Real Analytics destination: GET /analytics -> TelemetryDashboardController :show (router.ex:104-108) — never the prototype's aiur.team URL. New cards consume DASH-032's CurrentRunOutcomeSnapshot ONLY; keep the global RecentMerge presentation as a distinct scope (do not filter RecentOutcomes' durable list by observed_run_id — that's the exact anti-pattern the pack documents). Units destination (DASH-003) and Commands (DASH-007) do not exist yet — hard ordering.
