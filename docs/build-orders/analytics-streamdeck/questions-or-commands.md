# Questions or commands

Open items for the operator (none block launch):

1. Backlog closes awaiting approval: #1084 (54/54 members closed; reconciliation
   comment + post-merge proof unverified), #1311 (8/8 closed), #963 (premise
   deleted), #1175 (already fixed), #1315 (dup of #1325).
2. #1362 Part 1 (repository ruleset) requires operator/admin action on GitHub
   settings; the ticket delivers the verification check.
3. Dashboard credentials: Executor will set AIUR_DASHBOARD_USERNAME/PASSWORD in
   .env and report them in chat before launch (operator may supply preferred
   values).

Answered during planning: branch layout (three independent), model policy
(codex default, claude fallback), --debug authorized, merge-to-develop
authorized, self-fix boundary (tickets by default; own only fleet-blockers).
