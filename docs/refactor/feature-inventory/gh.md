## GitHub integration & PR monitoring (FI-GH)
The GitHub tracker client and config/auth, agent:* label machinery, issue dependencies, CODEOWNERS trust, review-thread reply/resolution, event detectors (firehose, comments poller, ls-remote ticker), connectivity resilience, and the pr_watch PR-monitoring pipeline (watch label, /aiur commands, PR-anchored agents).

- **FI-GH-001 · Boot-time token resolution with keyring fallback** [lifecycle] — resolve_token/1 runs once at boot BEFORE Orchestrator/GithubFirehose start: prefers a VALID GITHUB_TOKEN env var (validated via GET /rate_limit), falls back to the gh keyring token (queried with GITHUB_TOKEN/GH_TOKEN cleared), last-resorts to raw env; only a real binary token is cached in :persistent_term (caching nil would shadow later per-test env tokens); boot failure warns, never crashes startup (src/lib/aiur/github/config.ex:26-56, src/lib/aiur/github/config.ex:169-198, src/lib/aiur.ex:195-207). Tests: src/test/aiur/github/config_test.exs. Risk: high.
- **FI-GH-002 · Per-call token requirement + test-token seam** [api] — Opts-aware functions resolve token as: opts[:token] non-empty binary; else, if :request_fun present, the literal "test-gh-token" (injected tests need no env — load-bearing for the whole injected suite); else GitHub.Config.token() with {:error, :missing_github_token} on nil (src/lib/aiur/github/client.ex:2410-2429). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-003 · GitHub auth preflight** [lifecycle] — preflight_auth/1 probes rate_limit, repo, open issues in order and halts at first failure with {:error, {:github_auth_preflight_failed, diagnostic}}; diagnostic adds gh-keyring availability (checked with GITHUB_TOKEN cleared) and a recovery message that never leaks token material; Orchestrator refuses to start candidate polling on failure. Failure reasons include :invalid_or_expired_token, :repo_not_accessible, :rate_limited (also via /rate_limit body remaining==0), :forbidden, :request_failed (src/lib/aiur/github/client.ex:119-141, src/lib/aiur/github/client.ex:909-1019, src/lib/aiur/github/tracker.ex:34-43). Tests: src/test/aiur/github_client_test.exs, src/test/aiur/github_auth_preflight_test.exs. Risk: medium.
- **FI-GH-004 · Repo auto-detection from git origin** [config-option] — GitHub.Config.repo/0 returns github.repo when a non-blank string, else auto-detects owner/name from the git origin remote via Aiur.Git.origin_repo(); validate! requires a resolvable repo and token (src/lib/aiur/github/config.ex:10-24, src/lib/aiur/github/config.ex:146-158). Tests: none for the fallback (config_test.exs covers tokens only). Risk: medium.
- **FI-GH-005 · Label prefix configuration** [config-option] — label_prefix/0 reads github.label_prefix, defaulting to "agent" when unset/blank; every state label, the watch label, and issue-state extraction derive from it (src/lib/aiur/github/config.ex:8, src/lib/aiur/github/config.ex:58-70). Tests: src/test/aiur/github/labels_test.exs. Risk: low.
- **FI-GH-006 · bot_account and trusted_accounts config** [config-option] — bot_account/0 returns the trimmed login or nil (not required by validate!); trusted_accounts/0 accepts list or single string, trims, drops blanks, dedupes case-insensitively; both feed the CODEOWNERS allowlist and agent-login classification (src/lib/aiur/github/config.ex:72-116). Tests: src/test/aiur/github/code_owners_test.exs. Risk: low.
- **FI-GH-007 · pr_watch config block (opt-in, validated)** [config-option] — Defaults: enabled=false (aiur only reacts to its own aiur/<id> PRs), watch_label "watch" fully qualified as `<label_prefix>:<watch_label>` (agent:watch), command_prefix "/aiur"; blank watch_label or command_prefix rejected at config validation; a trusted comment starting with the prefix or mentioning bot_account wakes an agent with no label needed (src/lib/aiur/github/config.ex:118-144). Tests: src/test/aiur/config/pr_watch_test.exs. Risk: medium.
- **FI-GH-008 · GitHub request defaults (headers, timeout, etag)** [api] — Default Req request: Authorization Bearer, Accept application/vnd.github+json, X-GitHub-Api-Version 2022-11-28 (per-request :api_version override), 30s connect timeout, If-None-Match injected when :etag supplied; every client function accepts :request_fun; header lookup is case-insensitive and handles list and map shapes (src/lib/aiur/github/client.ex:2431-2493, src/lib/aiur/github/client.ex:2561-2583). Tests: none directly (all tests inject request_fun; real Req path untested). Risk: medium.
- **FI-GH-009 · GraphQL helper error surface** [api] — github_graphql/4: a 200 body containing "errors" returns {:error, {:github_graphql_errors, errors}} despite HTTP success; non-200 goes through the status/rate-limit classifier; transport errors classify into the taxonomy (src/lib/aiur/github/client.ex:1925-1941). Tests: src/test/aiur/github_client_test.exs. Risk: low.
- **FI-GH-010 · GitHub error taxonomy (classify_error)** [api] — classify_error/1 maps failures to {:github, class, detail}, class ∈ :dns|:timeout|:tls|:transport|:auth|:rate_limited|:http (:nxdomain→dns; timeout/closed/econnrefused/ehostunreach/enetunreach/econnreset→timeout; TLS alerts→tls; 401→auth; 403-with-rate-limit-signal and 429→rate_limited carrying retry_after and poll_interval from headers); non-rate-limited HTTP failures return {:github_api_status, status}. Connectivity backoff/escalation (#617) keys off these exact atoms — changing a classification silently changes alerting and retries (src/lib/aiur/github/client.ex:151-272). Tests: src/test/aiur/github/client_events_test.exs, src/test/aiur/github/connectivity_test.exs. Risk: high.
- **FI-GH-011 · Tracker behaviour adapter with swappable client** [api] — Aiur.GitHub.Tracker delegates every call to the module in Application env :github_client_module (default Aiur.GitHub.Client); project_identity/0 = configured repo; default_prompt_template/0 is a fixed Liquid template; auth_preflight tolerates clients exporting preflight_auth/0, /1, or neither (:ok); selected when tracker kind == "github". Orchestrator reads the same env key separately for verify_human_review_ready — both lookups must stay consistent (src/lib/aiur/github/tracker.ex:1-103, src/lib/aiur/tracker.ex:99, src/lib/aiur/orchestrator.ex:2806). Tests: src/test/aiur/tracker_github_test.exs. Risk: medium.
- **FI-GH-012 · Candidate issue fetch by state labels** [integration] — fetch_candidate_issues/1 issues one request per `<prefix>:<state>` label (GitHub label queries AND together) and dedups by issue id; state names normalized (trim, downcase, spaces→hyphens); empty state list short-circuits to {:ok, []} with no network call. A refactor that ANDs labels in one query would return zero candidates (src/lib/aiur/github/client.ex:274-288, src/lib/aiur/github/client.ex:1118-1153, src/lib/aiur/github/client.ex:2390-2395). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-013 · Fetch issue states by ids (404-tolerant)** [api] — fetch_issue_states_by_ids/2 fetches each issue individually, silently skips 404s (deleted issues drop out), halts on any other failure, preserves input order (src/lib/aiur/github/client.ex:284-288, src/lib/aiur/github/client.ex:1180-1207). Tests: src/test/aiur/github_client_test.exs. Risk: low.
- **FI-GH-014 · Issue normalization to Aiur.Issue** [api] — id/identifier = issue number as string; state = "Closed" when the GitHub issue is closed (overriding stale agent:* labels — prevents resurrecting closed tickets), else the suffix of the first `<prefix>:` label; priority from priority:N label; labels lowercased; assigned_to_worker always true; timestamps parsed or nil (src/lib/aiur/github/client.ex:2329-2388). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-015 · update_issue_state label swap lifecycle** [lifecycle] — GETs the issue, removes ALL `<prefix>:*` labels (delete tolerates 404), adds the single new normalized state label. On a closed issue with an active target state it strips active labels and does NOT add the new one; the add path re-checks closed-ness right before adding an active label to close a race window. This is the core agent:* state machine on GitHub (src/lib/aiur/github/client.ex:848-870, src/lib/aiur/github/client.ex:2057-2258, src/lib/aiur/github/client.ex:2312-2327). Tests: src/test/aiur/github_client_test.exs. Risk: high.
- **FI-GH-016 · Terminal state closes the issue** [lifecycle] — After the label swap, targets normalizing to done, cancelled, or canceled (both spellings terminal) PATCH the issue to state=closed; non-terminal states never touch open/closed (src/lib/aiur/github/client.ex:2290-2310, src/lib/aiur/github/client.ex:2325-2327). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-017 · add_label / remove_label primitives** [api] — add_label POSTs (2xx = ok); remove_label DELETEs treating 404 as success so toggling is idempotent; both delegated via Tracker for non-state labels (model:*, agent:watch) (src/lib/aiur/github/client.ex:872-905, src/lib/aiur/github/tracker.ex:90-98). Tests: none directly (exercised via tracker delegation tests). Risk: low.
- **FI-GH-018 · create_comment on issue** [api] — POSTs a comment body to /issues/{n}/comments; :ok on 200/201, logs and returns {:error, {:github_api_status, status}} otherwise, classified taxonomy on transport error (src/lib/aiur/github/client.ex:290-310). Tests: src/test/aiur/github_client_test.exs, src/test/aiur/tracker_github_test.exs. Risk: low.
- **FI-GH-019 · Canonical label families for init (label_set)** [artifact] — label_set/2 defines what `aiur init` seeds: state labels `<prefix>:{todo,in-progress,human-review,rework,merging,done,error,cancelled,canceled}`; `<prefix>:watch` (created but deliberately excluded from dispatch state suffixes); model labels for the CHOSEN backends only; model:remote alias only when claude is among them; complexity:1..5. @state_suffixes must stay in sync with Aiur.TestReset.reset_labels_command_args/1 (src/lib/aiur/github/labels.ex:26-74, src/lib/aiur/init.ex:18). Tests: src/test/aiur/github/labels_test.exs. Risk: medium.
- **FI-GH-020 · Label descriptions (describe/1)** [other] — Fixed human description per family: complexity:N → 'story-point complexity N'; model:remote → 'Supports claude remote-control'; model:X → 'route this issue to X'; prefixed state suffixes get fixed phrases; unknown suffix echoes itself; unprefixed unknown label → "" (src/lib/aiur/github/labels.ex:76-98). Tests: src/test/aiur/github/labels_test.exs. Risk: low.
- **FI-GH-021 · Idempotent label creation (ensure/5)** [lifecycle] — One POST per label against /repos/{o}/{r}/labels, stopping at the first hard failure; 422 whose body errors include 'already_exists' counts as success (re-running init is safe); other failures surface {:github_api_status, status, label} / {:github_api_request, reason}; own Req default with 2022-11-28 header (src/lib/aiur/github/labels.ex:100-150). Tests: src/test/aiur/github/labels_test.exs. Risk: low.
- **FI-GH-022 · Issue Dependencies REST wrappers with pinned API version** [api] — fetch_blocked_by/fetch_blocking GET /issues/{n}/dependencies/*; add_dependency POSTs {"issue_id": blocker internal id}, remove_dependency DELETEs, both against .../dependencies/blocked_by; ALL dependency calls send X-GitHub-Api-Version: 2026-03-10 (every other call uses 2022-11-28); add/remove take the blocker's internal numeric id, not its issue number (src/lib/aiur/github/client.ex:372-411, src/lib/aiur/github/client.ex:2495-2559). Tests: src/test/aiur/github/client_events_test.exs (incl. API version header). Risk: medium.
- **FI-GH-023 · fetch_issue_raw (blocker id resolution)** [api] — Returns the raw GitHub issue map (not Aiur-normalized) so IssueDependencies can read the internal numeric "id" required by the Dependencies API (src/lib/aiur/github/client.ex:413-431). Tests: src/test/aiur/github_issue_dependencies_test.exs (indirect). Risk: low.
- **FI-GH-024 · Dependency declare with BFS cycle pre-check (agent tool)** [api] — declare/3 backs aiur_declare_blocker: blocker id resolution (404→:blocker_not_found); idempotent {:ok, :already_present}; BFS over the blocking graph (visited set, 100-hop depth bound, 200-API-call budget) before POSTing. Budget exhaustion→:rate_limited; transient error mid-BFS→:cycle_check_inconclusive (never POSTs optimistically); mid-BFS 404 = leaf; reaching the current issue→:cycle_detected; POST 403→:permission_denied, 422→:cycle_detected (src/lib/aiur/github/issue_dependencies.ex:55-177, src/lib/aiur/agent_runner.ex:2067). Tests: src/test/aiur/github_issue_dependencies_test.exs. Risk: high.
- **FI-GH-025 · Dependency unblock (agent tool)** [api] — unblock/3 backs aiur_unblock: resolves the blocker id then DELETEs; 404 on DELETE → {:ok, :not_present} (idempotent), 403 → {:error, :permission_denied} (src/lib/aiur/github/issue_dependencies.ex:74-92, src/lib/aiur/agent_runner.ex:2091). Tests: src/test/aiur/github_issue_dependencies_test.exs. Risk: low.
- **FI-GH-026 · Canonical branch to open PR mapping** [integration] — fetch_open_pull_request_for_branch/2 finds the open PR whose head is `<owner>:aiur/<issue#>` (first match or nil): the ticket↔PR join used by the human-review gate and comment routing; renaming the branch scheme breaks PR discovery silently (empty result, not error) (src/lib/aiur/github/client.ex:515-541). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-027 · PR head-ref and open-PR lookup for comment routing** [api] — fetch_pull_request_head_ref/2 returns the head branch ({:error, :head_ref_missing} when absent); fetch_open_pull_request/2 returns the PR map only when open, and {:ok, nil} on 404 (plain issue) OR closed/merged — the nil is the routing signal to fall through to the legacy reactivation path instead of dispatching onto a dead branch (src/lib/aiur/github/client.ex:687-756). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-028 · CODEOWNERS-classified PR review / issue comments** [integration] — fetch_classified_pr_review_comments/2 (changed paths + review comments classified per-path via Aiur.Codeowners) and fetch_classified_issue_comments/2 (repo-wide ownership, no paths); classification marks comments authoritative or not, driving the review→rework loop (src/lib/aiur/github/client.ex:758-766, src/lib/aiur/github/client.ex:829-846). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-029 · Unaddressed review-thread comment extraction (GraphQL)** [integration] — Pages reviewThreads (100/page, cursors), keeps only UNRESOLVED threads, takes each thread's latest comment, CODEOWNERS-classifies it; agent-authored latest comments come back authoritative:true AND review_thread_resolution_required:true (an unresolved thread whose last word was the agent's must still be resolved); normalized to REST shape with review_thread_id/path/line. Feeds both rework dispatch and the human-review gate (src/lib/aiur/github/client.ex:768-787, src/lib/aiur/github/client.ex:1954-2055, src/lib/aiur/github/client.ex:86-117). Tests: src/test/aiur/github_client_test.exs. Risk: high.
- **FI-GH-030 · Verified review-thread reply with retry** [api] — reply_to_review_thread/3 posts addPullRequestReviewThreadReply then VERIFIES by re-fetching (latest comment bot-authored with the exact body); verification mismatch retries with linear sleep (250ms×attempt, default 3 attempts) WITHOUT reposting; retryable mutation errors (dns/timeout/tls/transport/rate_limited) retry the mutation itself; final failure {:review_thread_reply_not_verified, ...}. No-duplicate-reply-on-verify-retry is the key invariant — retrying the mutation on verify failure would spam reviewers (src/lib/aiur/github/client.ex:789-800, src/lib/aiur/github/client.ex:1304-1406, src/lib/aiur/github/client.ex:1862-1887). Tests: src/test/aiur/github_client_test.exs. Risk: high.
- **FI-GH-031 · Guarded review-thread resolution with rollback** [api] — resolve_review_thread/2 requires :terminal_reply_body and enforces preconditions: thread not already resolved, latest comment bot-authored with exactly the terminal body, latest non-agent reviewer comment CODEOWNERS-authoritative for the thread path. After resolving it re-verifies the terminal reply is STILL latest; a reviewer follow-up winning the race triggers a compensating UNRESOLVE (rollback) with details attached; FORBIDDEN/INSUFFICIENT_SCOPES/PAT errors classify as :review_thread_resolution_not_permitted with token guidance (src/lib/aiur/github/client.ex:802-811, src/lib/aiur/github/client.ex:1408-1564, src/lib/aiur/github/client.ex:1585-1734). Tests: src/test/aiur/github_client_test.exs. Risk: high.
- **FI-GH-032 · Bot identity resolution (config or viewer login)** [api] — Review-thread verification resolves identity as opts[:bot_account] → github.bot_account config → GraphQL viewer{login} of the token; codeowners_classification_opts merges bot_account into :agent_logins so the agent's own comments are never treated as reviewer feedback (src/lib/aiur/github/client.ex:1825-1851, src/lib/aiur/github/client.ex:2012-2049, src/lib/aiur/github/client.ex:78-84). Tests: src/test/aiur/github_client_test.exs. Risk: medium.
- **FI-GH-033 · human-review gate on unaddressed review threads** [lifecycle] — update_issue_state to human-review runs verify_issue_review_threads_clear first and fails with {:error, {:unverified_review_threads, %{issue_number, pr_number, review_thread_ids, comment_ids, count}}} unless the canonical aiur/<issue> PR has zero unaddressed thread comments; verify_human_review_ready/2 exposes the same check standalone (Orchestrator calls it via function_exported? — renaming/changing arity silently disables the gate); no open PR passes (src/lib/aiur/github/client.ex:813-827, src/lib/aiur/github/client.ex:2094-2149, src/lib/aiur/orchestrator.ex:2776-2781). Tests: src/test/aiur/github_client_test.exs, src/test/aiur/orchestrator_deactivate_test.exs. Risk: high.
- **FI-GH-034 · CODEOWNERS comment-trust allowlist GenServer** [lifecycle] — Supervision-tree singleton parses .github/CODEOWNERS into the author allowlist for event sanitization: @user tokens are logins, @org/team expands via the team-members API (a failed team resolution logs and skips that token only), path globs and emails ignored; bot_account + trusted_accounts ALWAYS unioned in; allowed?/2 case-insensitive, allowed?(nil) false; bootstrap sentinel entry so the first refresh is never mistaken for a populated list. This is the structural prompt-injection defense — unlisted authors get comment bodies dropped by the Sanitizer (src/lib/aiur/github/code_owners.ex:56-149, src/lib/aiur/github/code_owners.ex:246-304, src/lib/aiur.ex:110). Tests: src/test/aiur/github/code_owners_test.exs. Risk: high.
- **FI-GH-035 · Empty-allowlist fallback to repo owner + one-shot alert** [alert] — A refresh resolving a fully empty allowlist falls back to trusting only the repo owner (lowercased) and emits a one-shot needs_attention warning 'github.codeowners.allowlist_empty' (re-armed once a non-empty allowlist appears); underivable owner keeps the PREVIOUS allowlist and logs an error; missing/empty CODEOWNERS with configured trusted accounts logs a 'reduced to bot-only' warning instead. Encodes the #687/#693 fixes: deny-all kills the review→rework loop, trust-all opens injection (src/lib/aiur/github/code_owners.ex:151-230). Tests: src/test/aiur/github/code_owners_test.exs. Risk: high.
- **FI-GH-036 · Periodic CODEOWNERS refresh preserving allowlist on failure** [lifecycle] — Timer re-parse/re-resolve every refresh_seconds (opt, default 3600s), with manual refresh cancelling/rescheduling the timer so ticks never double; team-resolution failures during refresh skip the token rather than raising. Discrepancy to preserve as-is: the events.codeowners_refresh_seconds schema knob is NOT passed to the GenServer (started bare in the supervision tree, so the 3600s module default always applies) despite the moduledoc claiming otherwise (src/lib/aiur/github/code_owners.ex:62, src/lib/aiur/github/code_owners.ex:138-149, src/lib/aiur.ex:110, src/lib/aiur/config.ex:139-142). Tests: src/test/aiur/github/code_owners_test.exs. Risk: medium.
- **FI-GH-037 · Team member expansion with Link pagination** [api] — fetch_team_members/3 lists /orgs/{org}/teams/{team}/members at per_page=100 following Link rel="next" across all pages; 403 (missing read:org) surfaces as an error the caller logs and skips (src/lib/aiur/github/client.ex:433-482). Tests: src/test/aiur/github/code_owners_test.exs. Risk: low.
- **FI-GH-038 · Author trust stamping (conservative default false)** [lifecycle] — Sanitizer.stamp_author_trust/2 sets :author_trusted? from CodeOwners.allowed?(author) (CODEOWNERS ∪ bot_account ∪ trusted_accounts); CodeOwners process not running, nil author, or call exit ⇒ false (event stays operator-visible but is filtered from agent digests and command scanning); author from payload :author or opts[:actor]. Flipping the fail-closed default would let untrusted comments drive rework and PR command spin-up (src/lib/aiur/events/sanitizer.ex:102-121, src/lib/aiur/github/code_owners.ex:27-28, src/lib/aiur/github/code_owners.ex:233-238). Tests: src/test/aiur/events/sanitizer_test.exs, src/test/aiur/events/github_comments_poller_test.exs. Risk: high.
- **FI-GH-039 · Comment publish sanitization pipeline order** [lifecycle] — Every comment publish (poller and firehose alike) applies, in order: Map.put(:source, :github) → Sanitizer.scrub → stamp_author_trust(actor:) → put_comment_message; downstream renderers depend on :source for <external-content> wrapping and :author_trusted? for agent-digest filtering (src/lib/aiur/events/github_comments_poller.ex:298-312, src/lib/aiur/events/github_firehose.ex:178-185, src/lib/aiur/agent_runner.ex:1530). Tests: src/test/aiur/events/github_comments_poller_test.exs, src/test/aiur/events/sanitizer_test.exs. Risk: medium.
- **FI-GH-040 · ETag-conditional repo /events polling** [integration] — fetch_repo_events/1 GETs /repos/{o}/{r}/events with If-None-Match (GithubFirehose.poll uses page 1, per_page 30): 304 → {:not_modified, etag, poll_interval} so steady-state polling is rate-limit-free; the prior etag is preserved when GitHub omits the response ETag header on BOTH 200 and 304 (dropping this silently re-fetches and re-translates the same events every poll); X-Poll-Interval defaults to 60s; on API/transport error the orchestrator preserves the previous etag and last_event_id for the next tick (src/lib/aiur/github/client.ex:312-370, src/lib/aiur/events/github_firehose.ex:56-89, src/lib/aiur/orchestrator.ex:1327-1350). Tests: src/test/aiur/github/client_events_test.exs, src/test/aiur/events/github_firehose_test.exs, src/test/aiur/orchestrator_firehose_test.exs. Risk: high.
- **FI-GH-041 · Firehose event translation surface (narrow by design)** [event] — Only two GitHub event types publish: PushEvent on a non-ticket single-segment branch → 'system.<branch>.branch.push' (ticket-branch PushEvents deliberately IGNORED — LsRemoteTicker owns ticket pushes), and PullRequestEvent with an aiur/<digits> head: opened → 'ticket.<id>.pr.opened', closed+merged=true → 'ticket.<id>.pr.merged' (bypass_contamination: true only for merged so untracked human-review tickets still see their merge); all other event types and PR actions dropped silently (src/lib/aiur/events/github_firehose.ex:197-248, src/lib/aiur/events/github_firehose.ex:13-24). Tests: src/test/aiur/events/github_firehose_test.exs. Risk: medium.
- **FI-GH-042 · Firehose watermark + saturated-page backfill** [lifecycle] — poll tracks last_event_id; when page 1 is saturated (>=30 events) and lacks the watermark, pages 2..5 are fetched until the watermark appears or a page is unsaturated; only events strictly NEWER than the watermark publish (take_while by id); nil watermark → page 1 only; any backfill fetch error fails the whole poll WITHOUT advancing watermark or etag. Off-by-one here re-publishes or drops events (src/lib/aiur/events/github_firehose.ex:39-40, src/lib/aiur/events/github_firehose.ex:91-141). Tests: src/test/aiur/events/github_firehose_test.exs. Risk: high.
- **FI-GH-043 · Pre-boot event drop (restart replay suppression)** [lifecycle] — Firehose events with created_at older than boot minus a 60s buffer are dropped (:pre_boot_drop) before publish, preventing GitHub's ~24h event replay from firing phantom pr.opened/push into an empty dedup table after restart; events with no/invalid created_at are NOT dropped; boot_time injectable for tests. The same boot cutoff (boot − 60s, ISO8601) is the default since cursor for the comments poller and command scanner (src/lib/aiur/events/github_firehose.ex:149-163, src/lib/aiur/events/github_keys.ex:96-119). Tests: src/test/aiur/events/github_keys_test.exs; firehose tests inject boot_time. Risk: high.
- **FI-GH-044 · Firehose per-event publish crash isolation** [lifecycle] — A raise inside translate/sanitize/publish of a single event is rescued, logged as a warning, and returns :ignored — one malformed GitHub event cannot abort the rest of the batch or crash the poll (src/lib/aiur/events/github_firehose.ex:187-191). Tests: none. Risk: medium.
- **FI-GH-045 · Firehose polling cadence (X-Poll-Interval + Retry-After + backoff)** [lifecycle] — The orchestrator stores GitHub's X-Poll-Interval (seconds×1000) in github_poll_delays; the next :run_poll_cycle is scheduled at max(github_poll_delays values) falling back to polling.interval_seconds×1000; classified failures (DNS/auth/429-with-Retry-After) feed Connectivity backoff into the same delay map; success clears the source's delay (src/lib/aiur/orchestrator.ex:1490-1508, src/lib/aiur/orchestrator.ex:171, src/lib/aiur/github/client.ex:314-334). Tests: src/test/aiur/orchestrator_firehose_test.exs. Risk: high.
- **FI-GH-046 · Ref-to-topic classification (canonical aiur/<digits> only)** [api] — GithubKeys.ref_to_topic/1: 'refs/heads/aiur/<digits>' → {:ticket, id, 'ticket.<id>.branch.push'}; any OTHER single-segment branch → {:system, 'system.<branch>.branch.push'}; everything else (multi-segment refs, 'aiur/<id>-slug', 'aiur/abc', non-binary) → nil (no publish); shared by firehose, ls-remote ticker, and comments poller (src/lib/aiur/events/github_keys.ex:20-33). Tests: src/test/aiur/events/github_keys_test.exs, src/test/aiur/events/ls_remote_ticker_test.exs. Risk: medium.
- **FI-GH-047 · Nil-safe dedup key constructors** [api] — pr_dedup_key/comment_dedup_key/review_thread_dedup_key return a {repo, kind-qualifier, id} triple only when every argument has the right type; malformed input → nil, which Publisher treats as 'no dedup' (event passes through rather than crashing or being dropped) (src/lib/aiur/events/github_keys.ex:40-72, src/lib/aiur/events/publisher.ex:198-211). Tests: src/test/aiur/events/github_keys_test.exs, src/test/aiur/events/publisher_test.exs. Risk: medium.
- **FI-GH-048 · LsRemoteTicker: sole ticket-branch push detector** [integration] — GenServer polls `git ls-remote --heads origin 'refs/heads/aiur/*'` every 30s (:interval_ms/:remote/:ref_pattern opts) and publishes 'ticket.<id>.branch.push' when a known ref's SHA changes — the ONLY ticket push source (the firehose deliberately skips ticket pushes because /events omits low-traffic PushEvents; moving detection back regresses the documented aiur/99 failure). Payload: ref, sha, actor nil, commits [], repo resolved per tick via Tracker.project_identity when unconfigured (nil repo still publishes); blockee auto-resume depends on this (src/lib/aiur/events/ls_remote_ticker.ex:37-39, src/lib/aiur/events/ls_remote_ticker.ex:213-244, src/lib/aiur.ex:116). Tests: src/test/aiur/events/ls_remote_ticker_test.exs. Risk: high.
- **FI-GH-049 · LsRemoteTicker bootstrap suppression + error-safe baseline** [lifecycle] — The FIRST SUCCESSFUL tick records all current SHAs without publishing (no phantom pushes for pre-existing branches; logs 'aiur_perf ls_remote_ticker phase=bootstrap_done'); error ticks never set bootstrapped?, so error-then-success bootstraps on the success with no phantom publishes; from the second successful tick both changed SHAs and brand-new refs publish. Bootstrapping on an error with an empty cache would fan phantom auto-resumes to every paused blockee (src/lib/aiur/events/ls_remote_ticker.ex:101-110, src/lib/aiur/events/ls_remote_ticker.ex:185-211). Tests: src/test/aiur/events/ls_remote_ticker_test.exs. Risk: high.
- **FI-GH-050 · LsRemoteTicker crash resilience and always-on perf logging** [lifecycle] — run_tick wraps the shell-out and publish in rescue/catch so hiccups never kill the GenServer; the next tick is always scheduled (interval or classified backoff); publishes emit always-on 'aiur_perf ls_remote_ticker phase=publish_push ...' lines not gated on --debug (src/lib/aiur/events/ls_remote_ticker.ex:111-135, src/lib/aiur/events/ls_remote_ticker.ex:88-92). Tests: src/test/aiur/events/ls_remote_ticker_test.exs. Risk: medium.
- **FI-GH-051 · Connectivity streak escalation → system.github.connectivity_lost** [alert] — note_failure/3 tracks consecutive same-class failures per source in a caller-owned map; an alert struct is returned EXACTLY ONCE, when a :dns or :auth streak reaches 3 (count == 3, not >= — prevents alert spam); transient classes (:timeout, :rate_limited, :http, :tls, :transport) never escalate; class switch resets the streak; sources are independent; note_success/2 deletes the streak and re-arms. Firehose, comments poller, and ls-remote ticker all emit the single operator-visible custom alert 'system.github.connectivity_lost' (needs_attention: true, severity warning) via Alerts.emit_custom; a later success clears the streak so a new break re-escalates (src/lib/aiur/github/connectivity.ex:27-80, src/lib/aiur/orchestrator.ex:1437-1518, src/lib/aiur/events/ls_remote_ticker.ex:120-169). Tests: src/test/aiur/github/connectivity_test.exs, src/test/aiur/orchestrator_firehose_test.exs, src/test/aiur/events/ls_remote_ticker_test.exs. Risk: high.
- **FI-GH-052 · Classified backoff policy** [lifecycle] — backoff_ms/3: :auth → :escalate (never silently retry an expired token — callers map to max backoff); :rate_limited honors retry_after seconds, else poll_interval, else exponential; all other classes exponential 1s×2^(attempt−1); everything capped at 60s (src/lib/aiur/github/connectivity.ex:92-116). Tests: src/test/aiur/github/connectivity_test.exs. Risk: medium.
- **FI-GH-053 · git ls-remote failure classification** [api] — classify_ls_remote/1 maps git's English error text into the same taxonomy: 'could not resolve host'/'name or service not known'/'temporary failure in name resolution' → :dns; 'authentication failed'/'could not read username'/'permission denied' → :auth; 'timed out' → :timeout; else :transport — so git-level DNS outages escalate like API-level ones; the exact substrings are the contract (src/lib/aiur/github/connectivity.ex:118-148, src/lib/aiur/events/ls_remote_ticker.ex:120). Tests: src/test/aiur/events/ls_remote_ticker_test.exs (integration only; no direct unit coverage). Risk: medium.
- **FI-GH-054 · Connectivity operator alert message** [alert] — alert_message/2 formats the fixed operator string: 'GitHub <DNS resolution failures|authentication failures|...> from <source>[ for <repo>]: N consecutive failures. Agents cannot reach GitHub while the operator shell may. Check DNS/proxy/credentials in the agent workspace environment.' (src/lib/aiur/github/connectivity.ex:151-167, src/lib/aiur/orchestrator.ex:1511). Tests: none (text unasserted). Risk: low.
- **FI-GH-055 · Direct comment polling of watched tickets (3 endpoints per target)** [integration] — For each watched target the poller queries issue comments, the target's open-PR conversation comments, and its unresolved PR review threads directly (the repo /events feed is delayed/sampled); issue and PR-conversation comments publish 'ticket.<t>.issue.commented', review-thread comments 'ticket.<t>.pr.review_comment'; all publishes carry actor from user.login and bypass_contamination: true so deactivated (untracked) tickets can still be reactivated by an inbound human comment — dropping that flag silently kills comment-driven rework (src/lib/aiur/events/github_comments_poller.ex:128-141, src/lib/aiur/events/github_comments_poller.ex:251-296, src/lib/aiur/events/github_comments_poller.ex:298-312). Tests: src/test/aiur/events/github_comments_poller_test.exs. Risk: high.
- **FI-GH-056 · Comments poller target normalization + empty short-circuit** [api] — Targets are stringified, trimmed, blank-rejected, and deduplicated before polling; an empty (or all-blank) list returns {:ok, %{since:, count: 0, errors: []}} without any GitHub call (src/lib/aiur/events/github_comments_poller.ex:21-30, src/lib/aiur/events/github_comments_poller.ex:99-105). Tests: src/test/aiur/events/github_comments_poller_test.exs. Risk: low.
- **FI-GH-057 · Concurrent per-target polling with crash/timeout isolation** [lifecycle] — Targets polled via Task.Supervisor.async_stream_nolink on Aiur.TaskSupervisor (max_concurrency 4, per-target timeout 60s, on_timeout :kill_task; Task.async_stream under temporary trap_exit when the supervisor is absent); an exited/timed-out target yields a target-local {:target, {:exit, reason}} error with that target's cursor preserved, other targets unaffected; async_stream results are zipped back to targets in order — easy to break silently (src/lib/aiur/events/github_comments_poller.ex:17-18, src/lib/aiur/events/github_comments_poller.ex:69-97, src/lib/aiur/events/github_comments_poller.ex:43-51). Tests: src/test/aiur/events/github_comments_poller_test.exs. Risk: high.
- **FI-GH-058 · Per-target since cursor (advance only on clean poll, −1s overlap)** [lifecycle] — Each target keeps its own since cursor; after a zero-error poll it advances to the newest seen comment updated_at/created_at MINUS 1 second (deliberate overlap absorbed by Publisher dedup — shrinking the dedup TTL or removing the rewind reintroduces duplicate or lost comments); any error leaves that target's cursor unchanged so no comment is skipped; since input may be a per-target map, single ISO8601 string, or nil (boot cutoff) (src/lib/aiur/events/github_comments_poller.ex:111-126, src/lib/aiur/events/github_comments_poller.ex:354-364). Tests: src/test/aiur/events/github_comments_poller_test.exs. Risk: high.
- **FI-GH-059 · Agent Workpad comment exclusion** [event] — Issue and PR-conversation comments whose body (after leading-whitespace trim) starts with '## Agent Workpad' are never published — the agent's own workpad edits don't wake agents or spam digests; review-thread comments are NOT workpad-filtered (only the two issue-comment paths call CommentFilter) (src/lib/aiur/events/github_comments_poller.ex:148, src/lib/aiur/events/github_comments_poller.ex:221, src/lib/aiur/events/comment_filter.ex:5-12). Tests: src/test/aiur/events/github_comments_poller_test.exs (no dedicated comment_filter test file). Risk: medium.
- **FI-GH-060 · Open-PR resolution: caller-supplied PR map avoids re-fetch** [api] — If opts[:open_pull_requests_by_target] has the target as a KEY (Map.has_key?, even mapped to nil), the poller uses that value and never calls fetch_open_pull_request_for_branch; nil value = 'no open PR' and skips PR comment polling; absent key falls back to branch-based lookup; a PR map without a usable integer number is skipped without error. Converting to Map.get would re-fetch on nil and change API call volume (src/lib/aiur/events/github_comments_poller.ex:161-191, src/lib/aiur/events/github_comments_poller.ex:193-214). Tests: src/test/aiur/events/github_comments_poller_test.exs. Risk: medium.
- **FI-GH-061 · Unresolved review-thread wakeups (no since cursor, thread-id dedup)** [event] — Unaddressed PR review threads are fetched WITHOUT a since filter so an old unresolved thread still wakes the agent with no fresh comment timestamp; dedup key uses the stable GraphQL thread node id ((repo, 'pr_review_thread:<pr#>', thread_id)) when present, else the numeric comment id — one wakeup per thread per dedup TTL, so the Publisher's 1-hour TTL sets the re-nag cadence (src/lib/aiur/events/github_comments_poller.ex:234-249, src/lib/aiur/events/github_comments_poller.ex:289-296, src/lib/aiur/events/github_keys.ex:67-72). Tests: src/test/aiur/events/github_comments_poller_test.exs, src/test/aiur/events/github_keys_test.exs. Risk: high.
- **FI-GH-062 · Poll-cycle ordering of GitHub detectors** [lifecycle] — Each orchestrator :run_poll_cycle runs, in order: refresh running issue states → refresh tracked set → poll_github_firehose → poll_github_comments → scan_pr_commands → maybe_stop_closed_pr_anchored_agents → fetch candidates/dispatch; comments polling and command scanning only when tracker kind is 'github'; scan and teardown additionally require pr_watch.enabled. Tracked-set refresh BEFORE the firehose poll matters: the contamination filter consults the fresh set (src/lib/aiur/orchestrator.ex:2199-2205, src/lib/aiur/orchestrator.ex:1352-1357, src/lib/aiur/orchestrator.ex:1533-1539). Tests: src/test/aiur/orchestrator_firehose_test.exs, src/test/aiur/orchestrator_deactivate_test.exs. Risk: high.
- **FI-GH-063 · Comment-poll target selection: running + human-review/merging + agent:watch PRs** [lifecycle] — Targets are the deduped union of: identifiers of all running entries; idle tickets in human-review/merging states (capped 25/poll, priority-sorted, skipped when issue+PR updated_at is unchanged since last poll — a freshness gate saving PR lookups); and, when pr_watch.enabled, open PRs labeled `<label_prefix>:<watch_label>` keyed by PR number with the PR object passed through so the poller never re-fetches (capped 25, drops logged, closed/merged excluded). This is the PR-monitoring surface: dropping the label-discovery path or the unchanged-target skip changes both behavior and API volume (src/lib/aiur/orchestrator.ex:1885-1898, src/lib/aiur/orchestrator.ex:1907-1980, src/lib/aiur/orchestrator.ex:1993-2016). Tests: src/test/aiur/orchestrator_deactivate_test.exs. Risk: high.
- **FI-GH-064 · Watched-PR listing by label (agent:watch)** [integration] — fetch_open_pull_requests_by_label/2 lists ALL open PRs (per_page=100), filters client-side by label name (the /pulls endpoint has no server-side label filter), and follows Link rel=next pagination so watched PRs past page 1 are not dropped; returned maps keep number/head.ref/labels for the comment poller (src/lib/aiur/github/client.ex:543-595, src/lib/aiur/orchestrator.ex:1928). Tests: src/test/aiur/github/client_events_test.exs (incl. pagination). Risk: medium.
- **FI-GH-065 · Repo-wide one-off /aiur command scan (U3)** [lifecycle] — When pr_watch is enabled, each poll cycle fetches the repo-wide review-comment stream (/pulls/comments) AND conversation-comment stream (/issues/comments) — sort=updated desc, per_page=100, since cursor (default boot cutoff), Link pagination — because a /aiur review comment does not reliably bump PR updated_at, so per-PR freshness scanning would miss it. Comments get trust-stamped, PR numbers derived (pull_request_url tail for review comments; issue_url tail only when html_url contains '/pull/' — plain-issue comments dropped), filtered via PrCommandScanner, grouped by PR, capped at 25 distinct PRs/cycle (sorted, drops logged), and published as 'ticket.<pr#>.pr.review_comment' with bypass_contamination and dedup key (repo, 'pr_command:<pr#>', comment_id). The cursor advances over EVERY comment seen (newest −1s), not just hits, so a non-command comment cannot stall re-scanning; stream fetch failures yield [] without advancing; no label and no persistent watch state — one-and-done by construction (src/lib/aiur/orchestrator.ex:1520-1561, src/lib/aiur/orchestrator.ex:1622-1653, src/lib/aiur/orchestrator.ex:1766-1789, src/lib/aiur/github/client.ex:597-669). Tests: src/test/aiur/orchestrator_deactivate_test.exs, src/test/aiur/events/pr_command_scanner_test.exs; client stream fetchers untested directly. Risk: high.
- **FI-GH-066 · PR command detection: leading command prefix** [event] — A comment whose trimmed-leading body STARTS WITH the configured command prefix (e.g. '/aiur') is a command candidate; leading whitespace still matches; mid-body prefix does NOT; nil/blank prefix disables prefix matching only (mention matching unaffected) (src/lib/aiur/events/pr_command_scanner.ex:84, src/lib/aiur/events/pr_command_scanner.ex:90-97). Tests: src/test/aiur/events/pr_command_scanner_test.exs. Risk: low.
- **FI-GH-067 · PR command detection: word-bounded bot @-mention** [event] — '@<bot_account>' anywhere in the body is a candidate; match is case-insensitive, regex-escaped, and word-bounded on BOTH sides ('@aiur' does not match bot 'aiur-bot', '@aiur-bot' does not match bot 'aiur', email-like 'foo@aiur-bot' is not a mention); nil/blank bot_account disables mention matching. The regex '(^|[^A-Za-z0-9_/-])@login(?![A-Za-z0-9_-])' is precise and easy to break in a rewrite (src/lib/aiur/events/pr_command_scanner.ex:104-115). Tests: src/test/aiur/events/pr_command_scanner_test.exs. Risk: medium.
- **FI-GH-068 · PR command trust gate (consumes stamped author_trusted?)** [event] — A marker-bearing comment counts as a command only when its author_trusted? flag is true; the scanner does NOT re-derive trust — it reads the flag stamped upstream by Sanitizer.stamp_author_trust; both :author_trusted? and "author_trusted?" key forms honored; missing/false = not a command. This is the security boundary stopping untrusted users from spinning up agents via '/aiur' comments (src/lib/aiur/events/pr_command_scanner.ex:70-73, src/lib/aiur/events/pr_command_scanner.ex:117-119). Tests: src/test/aiur/events/pr_command_scanner_test.exs. Risk: medium.
- **FI-GH-069 · PR command bot self-loop drop** [event] — The bot's OWN '/aiur' or self-mention comments are never commands (case-insensitive, trimmed author-vs-bot_account comparison), so an agent-posted comment cannot re-trigger an agent; a HUMAN mentioning the bot still passes — the self-loop keys on the author, not the body (src/lib/aiur/events/pr_command_scanner.ex:71, src/lib/aiur/events/pr_command_scanner.ex:121-126). Tests: src/test/aiur/events/pr_command_scanner_test.exs. Risk: medium.
- **FI-GH-070 · PR command author resolution across REST and GraphQL shapes** [api] — Author resolved from ["user","login"] (REST issue/PR comments), falling back to ["author","login"] (GraphQL review-thread comments), then :author; commands/3 filters a list preserving input order; non-map or bodyless comments are never commands (src/lib/aiur/events/pr_command_scanner.ex:128-139, src/lib/aiur/events/pr_command_scanner.ex:55-57). Tests: src/test/aiur/events/pr_command_scanner_test.exs. Risk: low.
- **FI-GH-071 · Code-owner-comment-triggered agent spin-up (U4: PR-anchored dispatch)** [lifecycle] — A trusted comment event ('ticket.<N>.pr.review_comment' / issue.commented) with no running entry, pr_watch enabled, resolves N via GET /pulls/N: an OPEN PR whose head is NOT 'aiur/<N>' becomes a synthetic PR-anchored work unit (Issue id 'pr-<N>', identifier '<N>', state 'pr-watch', branch = the human head ref) dispatched through the slot-respecting worker path — gated on available_slots, skipping tracker revalidation, touching NO agent:* label and writing NO persistent state (cap-full/already-running logs and skips; the next comment re-triggers). Everything else (plain issue/404, closed PR, aiur/<N>-headed legacy PR, fetch error) falls through byte-for-byte to the legacy idle-rework transition — the aiur/<N> head-ref exclusion keeps legacy PR reactivation unchanged; a follow-up comment while the agent runs resumes the SAME session via find_running_by_identifier, never re-dispatches (src/lib/aiur/orchestrator.ex:610-640, src/lib/aiur/orchestrator.ex:647-717, src/lib/aiur/orchestrator.ex:740-765). Tests: src/test/aiur/orchestrator_deactivate_test.exs (pr_anchored_dispatch_fun capture tests). Risk: high.
- **FI-GH-072 · Closed-PR mid-run teardown of PR-anchored agents (U6)** [lifecycle] — Each poll cycle (pr_watch enabled, and only when PR-anchored entries exist — zero entries means zero fetches), every RUNNING entry in the 'pr-watch' sentinel state gets its PR re-fetched: {:ok, nil} (closed/merged) terminates the agent, removes the 'pr-<N>' workspace, and clears the PR-number-keyed session resume handle so a reopened PR won't --resume a finished thread (#613); {:ok, pr} and {:error, _} both leave the agent running (never terminate on a transient fetch error); untagging a watched PR mid-run does NOT abort the running agent — it only stops new dispatches (src/lib/aiur/orchestrator.ex:1655-1759). Tests: src/test/aiur/orchestrator_deactivate_test.exs (maybe_stop_closed_pr_anchored_agents_for_test). Risk: high.
