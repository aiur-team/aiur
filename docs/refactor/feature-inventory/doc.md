## Doc-drift: documented but absent or contradicted (FI-DOC)
Documentation claims with no implementation behind them, or directly contradicted by the code — each needs an explicit restore-or-fix-docs decision during the refactor rather than silent inheritance (these are NOT features to preserve as documented).

- **FI-DOC-001 · --debug chat-pane ANSI recorder** [doc-drift] — AGENTS.md:313-345 documents that `aiurdev --debug` auto-records each `OC | <issue>` chat pane into a stitched ANSI transcript at `log/record/chat.<issue>.ansi` (capture-pane -e, largest-overlap stitching, read-only, attach-to-detach) and calls it "the only durable record" of chat rendering; no such writer/stitcher exists in src/lib, src/scripts/aiurdev, or packaging/npm/aiur-cli/libexec/aiur-engine.sh — only the unrelated AIUR_DEBUG pipe-pane capture. Decide: restore the recorder or rewrite AGENTS.md:313-345. Tests: none. Risk: high. Check: run `aiurdev --debug`, open a chat pane, then look for `log/record/chat.<issue>.ansi` under the session log dir — nothing is ever written.
- **FI-DOC-002 · --record screen.ansi TUI recorder** [doc-drift] — A `--record` flag capturing the AgentList pane into `<session>/log/record/screen.ansi` is specified as shipped scope (docs/plans/2026-06-14-002-feat-logging-observability-rework-plan.md:210-232, docs/brainstorms/2026-06-14-logging-observability-rework-requirements.md:48) and the refactor spike plan even lists `--record` as a known feature to spot-check (docs/plans/2026-07-06-001-refactor-production-readiness-planning-spike-plan.md:451), but no `--record` exists in the engine usage/flag parse (packaging/npm/aiur-cli/libexec/aiur-engine.sh:270) or src/scripts/aiurdev. Decide: implement/reland it or strike it from plan verification lists. Tests: none. Risk: medium.
- **FI-DOC-003 · SPEC claims tracker.kind supports only "linear"** [doc-drift] — SPEC.md:375-377 states the current supported `tracker.kind` value is `linear`, contradicted by the shipped github and memory adapters (src/lib/aiur/tracker.ex:97-103 dispatch; README.md:25-27, src/README.md:118-121). Decide: fix SPEC 5.3.1 to name all three kinds (and the any-other-kind→Linear fallback) so a SPEC-guided refactor cannot treat github/memory as out-of-contract. Tests: none for the doc claim (adapter behavior tested in src/test/aiur/tracker_github_test.exs). Risk: medium.
- **FI-DOC-004 · SPEC `.aiurhooks` scaffold naming** [doc-drift] — SPEC.md:370 says `aiur init` scaffolds `.aiurhooks` (from `.aiurhooks.example`) referenced via `hooks_file: .aiurhooks`; init actually writes `.aiur/hooks` from the embedded `hooks.example` and references it as `hooks_file: hooks` (src/lib/aiur/init.ex:76-82; src/README.md:86-87). Decide: fix SPEC to the `.aiur/` folder layout. Tests: none. Risk: low.
- **FI-DOC-005 · SPEC `/refresh` "operational trigger" vs writable gate** [doc-drift] — SPEC.md:1580,1599 present POST /api/v1/refresh as an always-available operational trigger, but the router pipes it through `require_dashboard_writable`, default-off pending the #371 dashboard parity pass (src/lib/aiur_web/router.ex:36-65,126-138), so the documented trigger is refused out of the box. Decide: document the gate in SPEC (gating is deliberate) or exempt `/refresh` from it. Tests: none for the SPEC claim (gating covered by src/test/aiur_web/router_auth_test.exs). Risk: medium.
- **FI-DOC-006 · aiur-run skill: stale "no --max-agents flag yet"** [doc-drift] — .claude/skills/aiur-run/SKILL.md:37 tells operators to set concurrency only via `.aiur/config` because there is "no `--max-agents` flag yet — see #449", but the engine now accepts and documents `--max-agents <n>` (packaging/npm/aiur-cli/libexec/aiur-engine.sh:270). Decide: fix the skill's pre-flight step. Tests: none. Risk: low.
- **FI-DOC-007 · Website platform claim vs dropped darwin-x64 package** [doc-drift] — The quickstart (website/index.html:44-68) sells `npm i -g aiur-cli` with no platform qualification, but darwin-x64 (Intel Mac) builds are disabled in .github/workflows/release-npm.yml:25-30 (no macos-13 runner) while packaging/npm/aiur-cli/package.json:31 still lists the unpublished `aiur-cli-darwin-x64` optional dep. Decide: restore Intel-mac builds or qualify the site and prune the optional dep. Tests: none. Risk: low.
