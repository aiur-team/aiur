## Skills (driver, workspace-installed & codex-native) (FI-SKL)
Agent- and operator-facing skill documents under `.claude/skills/` and `.codex/skills/`, the behavioral contracts they encode (turn workflow, dev loop, events, monitoring, release), and the runtime machinery that installs/mirrors them into agent workspaces.

- **FI-SKL-001 · using-aiur operating-manual router** [skill] — `using-aiur/SKILL.md` is the agent's per-ticket manual: a router table to four reference docs (turn-workflow.md, dev-loop.md, complexity-routing.md, conventions.md) plus an aiur-agent pointer and a "shortest version" summary; the per-turn prompt carries only a pointer here, so a removed/renamed routed doc strands agents (.claude/skills/using-aiur/SKILL.md:13-35, src/prompts/shared-agent-instructions.md:4-8). Tests: src/test/aiur/aiur_agent_skill_test.exs. Risk: low.
- **FI-SKL-002 · aiur-agent cross-ticket events router** [skill] — `aiur-agent/SKILL.md` is the single source of truth for cross-ticket events: routes to five reference docs and carries a quick reference for emit_event, aiur_subscribe, aiur_declare_blocker, and attention open/close; promises tools fail loudly with structured errors (e.g. event_name_not_in_allowlist) (.claude/skills/aiur-agent/SKILL.md:12-27, src/prompts/shared-agent-instructions.md:15-20). Tests: src/test/aiur/aiur_agent_skill_test.exs:108-117. Risk: low.
- **FI-SKL-003 · aiur-agent reference-doc routing integrity** [skill] — SKILL.md must point at all five reference docs (overview.md, event-taxonomy.md, emit-and-subscribe.md, attention-and-resolve.md, stub-then-fetch.md) and each must exist on disk, since the pre-prompt sends agents to the skill (src/test/aiur/aiur_agent_skill_test.exs:19-25,108-117). Tests: src/test/aiur/aiur_agent_skill_test.exs:108-117. Risk: low.
- **FI-SKL-004 · Per-turn prompt → skill routing partition** [other] — The shared per-turn prompt deliberately carries only pointers ("using-aiur is your operating manual"; "load /aiur-agent before emitting events"), while two protocols intentionally stay IN the prompt: progress/progress.checkin operator-bar estimates and cross-ticket coordination reflexes; the partition itself is contractual (#382/#689 intent) (src/prompts/shared-agent-instructions.md:4-20, .claude/skills/using-aiur/SKILL.md:37-46). Tests: src/test/aiur/aiur_agent_skill_test.exs:119-152. Risk: medium.
- **FI-SKL-005 · Skill trigger-phrase descriptions (discovery contract)** [skill] — SKILL.md frontmatter descriptions double as discovery triggers: aiur-run must contain 'run IAR'/'run aiur'/'iarc run', aiur-monitor 'aiur status'/'iarc status'/'aiurdev watch', both 'iarc' and 'alias for `aiur`' — exact strings test-asserted (.claude/skills/aiur-run/SKILL.md:3, .claude/skills/aiur-monitor/SKILL.md:3). Tests: src/test/aiur/aiur_agent_skill_test.exs:154-169. Risk: low.
- **FI-SKL-006 · agent:* label lifecycle** [lifecycle] — GitHub issue state uses exactly: agent:todo, agent:in-progress, agent:human-review, agent:rework, agent:merging, agent:done, agent:cancelled, agent:canceled (both spellings). Turn flow: read issue, todo → in-progress, work, flip to human-review when PR ready; Done only when the issue says close without review; never self-merge (.claude/skills/using-aiur/turn-workflow.md:5-33, .claude/skills/using-aiur/SKILL.md:25-27). Tests: none. Risk: medium.
- **FI-SKL-007 · Agent Workpad single-comment protocol** [other] — Exactly ONE persistent issue comment titled '## Agent Workpad', updated in place: plan, validation, PR URL, blockers, handoff (phase/decisions/completed validation/next steps), and a fenced `<hostname>:<abs-workdir>@<short-sha>` block; handoff must be updated before ending a turn on an active issue (.claude/skills/using-aiur/turn-workflow.md:22-24,109-138). Tests: none. Risk: medium.
- **FI-SKL-008 · PR review feedback loop (reply-vs-code, verified thread replies)** [other] — Agents reply on threads by default, push code only on clear change intent; outdated threads still count. Before flipping back to agent:human-review, re-fetch the thread and confirm the agent's reply is latest, else stay agent:rework; replies via aiur_reply_review_thread, aiur_resolve_review_thread only after a verified terminal reply with that exact body; three named refusal errors (review_thread_resolution_not_permitted/_precondition_failed/_not_authorized) mean record-and-move-on (.claude/skills/using-aiur/turn-workflow.md:36-63). Tests: src/test/aiur/aiur_agent_skill_test.exs:34-45. Risk: medium.
- **FI-SKL-009 · PR-anchored mode (watched/commanded PRs)** [other] — When woken for a PR it didn't create (agent:watch or an addressed comment), the agent is checked out on that PR's own branch (not aiur/<id>): push to that same branch, reply on its threads, do NOT open a new PR, do NOT resolve threads unless told; CODEOWNERS trust still applies (.claude/skills/using-aiur/turn-workflow.md:65-71). Tests: src/test/aiur/aiur_agent_skill_test.exs:34-45. Risk: low.
- **FI-SKL-010 · Milestone phase alerts via emit_alert** [alert] — Agents emit phase.{brainstorm,plan,work,review}.{start,end} through emit_alert when actually entering/leaving each CE phase, always with name/message/reason/needs_attention (false for milestones); never emit Aiur-owned namespaces (task.*, agent.*, chat.*); Aiur auto-scopes agent names under ticket.<issue>.agent. so agents pass bare names (.claude/skills/using-aiur/turn-workflow.md:77-107). Tests: none. Risk: medium.
- **FI-SKL-011 · Complexity routing tiers (complexity:1..5)** [skill] — complexity:N routes model + CE-skill depth: 1-2 Codex/ce-work (+ce-code-review at 2); 3 Codex default (Claude for concurrency/persistence), plan→work→review; 4 latest Claude, full plan + ce-doc-review pushed as review artifact; 5 latest Claude with model_reasoning_effort=high, full brainstorm→…→review with adversarial personas. Missing label = complexity:3, never block; the label is an overridable hypothesis (.claude/skills/using-aiur/complexity-routing.md:1-85). Tests: none. Risk: medium.
- **FI-SKL-012 · CODEOWNERS comment-authority rules** [other] — Commenters classified via CODEOWNERS (.github/CODEOWNERS, then CODEOWNERS, then docs/CODEOWNERS; last matching rule per touched file wins): owners are authoritative, conflicting owners = pause for human direction, non-owners advisory, no file = all authoritative fallback, own comments never authoritative; classification noted in the workpad (.claude/skills/using-aiur/conventions.md:3-22). Tests: none. Risk: medium.
- **FI-SKL-013 · Out-of-scope findings protocol (needs-triage)** [other] — A real but out-of-scope problem is never silently fixed in the same PR: open a new issue labeled needs-triage referencing the current issue, and comment a link on the current issue (.claude/skills/using-aiur/conventions.md:24-39). Tests: none. Risk: low.
- **FI-SKL-014 · Branch naming contract: exactly aiur/<N>** [other] — Workspace branch is exactly aiur/<N> — never a slug, never renamed; a closed PR for aiur/<N> means push to the SAME branch and open a new PR. Load-bearing: blocker→blockee push detection keys branch.push on aiur/<N>, so an invented name means blockees never wake (.claude/skills/using-aiur/dev-loop.md:5-12, .claude/skills/using-aiur/SKILL.md:31). Tests: none. Risk: high. Check: during a run, `git -C <workspace> branch --show-current` on a dispatched agent's workspace and confirm it is exactly `aiur/<issue-number>`.
- **FI-SKL-015 · Pre-PR verification gate (five checks)** [other] — Before opening/finalizing a PR: mix compile --warnings-as-errors, mix format --check-formatted, mix test, mix credo --strict, mix dialyzer — fix every failure, never substitute a smaller gate, re-run the full gate after review fixes touching code/tests/prompt/skill/config; wording must stay in sync with .aiur/prompt.md (.claude/skills/using-aiur/dev-loop.md:37-44,59-60). Tests: src/test/aiur/aiur_agent_skill_test.exs:171-187. Risk: low.
- **FI-SKL-016 · Commit message + co-author conventions (Claude side)** [other] — Commits short (3-7 words), plain, human; NEVER mention Claude/Codex/AI/models/"generated with" in commits or PR descriptions; its-applekid-authored commits append 'Co-authored-by: its-everdred <kevinweaver2@gmail.com>', its-everdred commits need no trailer (.claude/skills/using-aiur/dev-loop.md:45-49, .claude/skills/using-aiur/SKILL.md:32-33). Tests: none. Risk: medium.
- **FI-SKL-017 · Draft-PR self-review flow + turn-end on human-review** [lifecycle] — PRs open DRAFT with --head aiur/<N>, self-reviewed via ce-code-review against the pushed diff, fixes committed, then marked ready + agent:human-review; never self-merge. Flipping the label ends the turn loop — no polling gh pr view; Aiur resumes on label flip-back; if idle but agent:in-progress, emit pause.request (.claude/skills/using-aiur/dev-loop.md:52-70). Tests: none. Risk: high. Check: watch a ticket flip to agent:human-review and confirm the agent's turn ends with no subsequent `gh pr view` polling in its transcript.
- **FI-SKL-018 · PR description contract (Closes #N + Problem/Solution)** [other] — Every PR description STARTS with Closes #N (or Fixes/Resolves; multiple: 'Closes #43, #46') for GitHub auto-close, then concise Problem/Solution; no complexity-routing explanations, model names, AI tool names, or process narratives (.claude/skills/using-aiur/dev-loop.md:84-108, .claude/skills/using-aiur/SKILL.md:32). Tests: none. Risk: low.
- **FI-SKL-019 · Manual CLI verification before draft PR** [other] — Before the draft PR, run the CLI locally and manually exercise all new functionality end-to-end, debugging failures rather than skipping; additive to (not a substitute for) the five-check gate (.claude/skills/using-aiur/dev-loop.md:72-81). Tests: none. Risk: medium.
- **FI-SKL-020 · Git index-lock recovery rule (never clone to /tmp)** [other] — On 'Could not write index'/'Unable to lock'/'cannot create FETCH_HEAD': rm -f .git/index.lock, re-run, then commit uncommitted edits and re-attempt — never stash, never clone a recovery checkout into /tmp (costs full deps+compile), never push from /tmp (.claude/skills/using-aiur/dev-loop.md:14-24). Tests: none. Risk: medium.
- **FI-SKL-021 · Blocker-branch integration sequence (commit WIP, no stash)** [other] — On ticket.<blocker>.branch.push: git fetch origin aiur/<blocker-id>, commit local WIP, git merge --no-edit origin/aiur/<blocker-id>, resolve, continue; explicitly do NOT git stash first (stash triggers the index-write failure path) (.claude/skills/using-aiur/dev-loop.md:27-31). Tests: src/test/aiur/aiur_agent_skill_test.exs:119-152. Risk: medium.
- **FI-SKL-022 · Pre-configured tooling environment convention** [other] — Agents rely on Aiur pre-setting HEX_HOME, MIX_HOME, MISE_TRUSTED_CONFIG_PATHS per workspace (mise trust already run) and must run mix/mise exec directly without inventing env prefixes that would bypass the pre-warmed Hex cache (.claude/skills/using-aiur/conventions.md:41-53). Tests: none. Risk: medium.
- **FI-SKL-023 · Synthetic load repro caps** [other] — Flake repros prefer deterministic techniques; if synthetic load is truly needed, generator workers cap at max(1, cores/4), stop promptly, and fixed high counts (e.g. yes x16) are forbidden on the shared host (.claude/skills/using-aiur/conventions.md:55-61). Tests: none. Risk: low.
- **FI-SKL-024 · Agent-emittable event name allowlist + topic scoping** [api] — emit_event accepts exactly: progress.<slug>, decision.<slug>, blocked, unblocked, attention.<slug>, attention.resolved, pause.request, custom.<slug> — else rejected; each maps to ticket.<id>.agent.<name>. Bare progress and progress.checkin also allowed (operator progress-bar protocol, documented in the pre-prompt). Doc is the agent-readable half of the bridge-enforced allowlist (#382) (.claude/skills/aiur-agent/event-taxonomy.md:17-36). Tests: src/test/aiur/aiur_agent_skill_test.exs:96-106. Risk: medium.
- **FI-SKL-025 · custom.* per-turn event quota** [config-option] — custom.* events capped at events.custom_events_per_turn_max (default 5) per turn; the 6th call returns custom_event_quota_exceeded; named categories have no quota (.claude/skills/aiur-agent/event-taxonomy.md:60-62). Tests: none in this slice (enforcement in events code). Risk: low.
- **FI-SKL-026 · emit_event tool contract (args, return, error shape)** [api] — emit_event(name, message, payload?) validates against the allowlist; returns {ok:true, name, message, result:{id, topic}} (monotonic id via Aiur.Events.IdGenerator, fully-scoped ticket.<id>.agent.* topic) or {error:{message, examples:[...]}} (.claude/skills/aiur-agent/emit-and-subscribe.md:3-28, .claude/skills/aiur-agent/overview.md:9). Tests: none in this slice. Risk: medium.
- **FI-SKL-027 · aiur_subscribe / aiur_unsubscribe (persistent, AMQP wildcards)** [api] — aiur_subscribe(topic_pattern) persists across BEAM restarts (SubscriptionStore-backed durability promise) with AMQP semantics (* = one segment, # = zero or more); aiur_unsubscribe is exact-match, no-op for unknown patterns; anti-patterns are contractual (no one-time-read binds, no bare #, no declare_blocker + ticket.N.#) (.claude/skills/aiur-agent/emit-and-subscribe.md:30-49,66-70). Tests: none in this slice. Risk: medium.
- **FI-SKL-028 · Default subscriptions + declare_blocker auto-subscription** [event] — Agents auto-subscribe to system.<default-branch>.branch.push; aiur_declare_blocker(N) records the dependency on GitHub AND auto-subscribes to a useful subset of ticket.N.* (progress, decisions, branch pushes, unblock signals); for a declared blocker, ticket.N.branch.push is an ACTION cue: fetch, inspect, stack (.claude/skills/aiur-agent/emit-and-subscribe.md:51-64, .claude/skills/aiur-agent/stub-then-fetch.md:12). Tests: src/test/aiur/aiur_agent_skill_test.exs:119-152. Risk: medium.
- **FI-SKL-029 · Event delivery contract (at-least-once, digest, mid-turn drain allowlist)** [event] — Delivery is at-least-once with renderer dedupe by event id (twice-delivered digest renders once); events land between turns as a digest, except the narrow mid-turn drain allowlist — ticket.<blocker>.branch.push, ticket.<blocker>.agent.unblocked, ticket.<blocker>.agent.decision.* — drained at the next safe checkpoint (.claude/skills/aiur-agent/overview.md:22-25). Tests: none in this slice (runner-side tested elsewhere). Risk: high. Check: push to a declared blocker's aiur/<N> branch mid-turn of a dependent agent and confirm the event surfaces in-turn, not just in the next digest.
- **FI-SKL-030 · System event sources + don't-emit corollary** [integration] — Four documented sources: GitHub firehose (branch.push, pr.opened, pr.merged, issue.commented, pr.review_comment), git ls-remote low-latency branch.push override, GitHub dependencies API (issue.blocked_by.changed), and agent emissions; agents never emit progress.commit/push, pr.opened/merged, or issue.commented — the firehose owns those (issue comments CODEOWNERS-filtered) (.claude/skills/aiur-agent/overview.md:27-34, .claude/skills/aiur-agent/event-taxonomy.md:38-58). Tests: none in this slice. Risk: medium.
- **FI-SKL-031 · Attention open/close lifecycle (exclamation chip)** [tui-behavior] — emit_event('attention.<slug>', msg) shows a chip in the agent row's second emoji slot (N-count when multiple; Enter expands slug+message+timestamp); ONLY the agent's own attention.resolved with payload.slug exactly matching clears it — operator reading/replying never does. Reflexes: max ~2 open, resolve before unrelated work (.claude/skills/aiur-agent/attention-and-resolve.md:5-39). Tests: none in this slice (agent-list rendering tested under src/test/aiur/agent_list/). Risk: medium.
- **FI-SKL-032 · Stub-then-fetch blocked-work protocol** [skill] — declare_blocker(N) never parks the ticket: keep caller-side prep moving, optionally write a LOCAL-ONLY stub (never pushed over ticket-N-owned files), emit unblocked with {temporary_stub:true, stubbed_function}; on ticket.N.branch.push fetch/inspect/stack, delete stub, re-test, re-emit unblocked; non-stubbable → emit blocked with {stubbable:false, reason}, park only dependent code; never poll (.claude/skills/aiur-agent/stub-then-fetch.md:1-53). Tests: src/test/aiur/aiur_agent_skill_test.exs:119-152. Risk: low.
- **FI-SKL-033 · aiur-run background-run playbook (operator skill)** [skill] — Detached --bg dogfood run pre-flight: refuse nested tmux ($TMUX blocks aiurdev), pull main, pgrep/stop stale BEAMs, set agent.max_concurrent_agents + pre_warmed_sessions in .aiur/config (equal-values rule until #376), NEVER aiurdev init --force, check agent:todo non-empty; build via aiurdev build; launch aiurdev --bg --debug; --test/--test3 forbidden for real runs (.claude/skills/aiur-run/SKILL.md:30-60). Tests: src/test/aiur/aiur_agent_skill_test.exs:154-169 (trigger phrases only). Risk: high.
- **FI-SKL-034 · aiur-run prewarm verification contract** [artifact] — Every launch verifies the warm base: ~/.aiur/repo/<owner>/<name>/.aiur-base-built exists with _build + deps; log reaches 'prewarm:phase ... :ready' (failure logs 'prewarm base unavailable:', #441); workspaces materialize quickly under workspace.root via CoW; cold-clone/build fallback means stop the run; the eager gate holds dispatch until :ready (.claude/skills/aiur-run/SKILL.md:25-28,62-72). Tests: none in this slice (RepoBase tested elsewhere). Risk: high. Check: after launch, `ls ~/.aiur/repo/<owner>/<name>/.aiur-base-built` and grep the run log for 'prewarm:phase' reaching ':ready'.
- **FI-SKL-035 · aiur-run monitoring handoff (cadence ask + immediate arming)** [skill] — Launch isn't done until cadence is armed: ask ONCE via AskUserQuestion (5/10/15 min, default 5), then immediately arm /loop <chosen>m /aiur-monitor (un-armed cadence = failed launch; no self-ticking fallback), arm watch-alerts.sh once via Monitor persistent:true, watch CPU/FDs (EMFILE, #409), investigate stuck agents (.claude/skills/aiur-run/SKILL.md:74-105). Tests: none. Risk: high. Check: immediately after a launch, confirm an active /loop for /aiur-monitor and a persistent Monitor task running watch-alerts.sh.
- **FI-SKL-036 · aiur-run lifecycle controls** [cli-command] — Run lifecycle via scripts/aiurdev engine subcommands: pause --all / resume --all (with #438 caveat: a masked rpc error can read as 'no running node' — cross-check aiurdev status), status, message <agent>, stop (reaps BEAM + tmux + agent sockets); identity per-instance-keyed by repo root (#431) so two instances coexist (.claude/skills/aiur-run/SKILL.md:15-28,107-114). Tests: none in this slice (subcommands tested in src/test/aiur_engine_test.exs). Risk: medium.
- **FI-SKL-037 · iarc operator alias** [cli-command] — aiur-run and aiur-monitor both declare 'iarc' as an operator alias for 'aiur': '/iarc run' triggers the run playbook, '/iarc status' the status playbook; the exact trigger phrases are contractual (.claude/skills/aiur-run/SKILL.md:12-13, .claude/skills/aiur-monitor/SKILL.md:12-13). Tests: src/test/aiur/aiur_agent_skill_test.exs:154-169. Risk: low.
- **FI-SKL-038 · aiur-monitor status board (aiurdev watch relay)** [skill] — Board compiled server-side: aiurdev watch --changes (deltas + actionables, low-token, '(no changes)' when steady) or --full (whole board); one row per active agent (TICKET · STATE · CX · AGE · DOING) plus ACTIONABLE section; stuck detection (>10m), complexity, tracker state computed server-side; the skill posts output AS-IS, never re-deriving (.claude/skills/aiur-monitor/SKILL.md:8-36,123-130). Tests: src/test/aiur/aiur_agent_skill_test.exs:163 ('aiurdev watch' referenced). Risk: medium.
- **FI-SKL-039 · aiur-monitor 'Needs you' line + daemon-down rendering** [alert] — Non-empty ACTIONABLE adds exactly ONE line — 'Needs you: #<id> (<why>)' naming a concrete action; empty ACTIONABLE OMITS the line entirely (never all-clear reassurance); the exact engine string 'aiur: orchestrator is not running' triggers a 'daemon down' line (not 'no active agents') plus restart advice (.claude/skills/aiur-monitor/SKILL.md:39-47). Tests: none. Risk: high. Check: stop the daemon and run aiurdev watch — confirm the monitor renders 'daemon down', not 'no active agents'.
- **FI-SKL-040 · Monitoring cadence contract (armed timer, no-skip, terminal condition)** [lifecycle] — While a run is live, a fresh board posts every chosen interval via an ARMED recurring timer (/loop <chosen>m /aiur-monitor); ticks never skipped ('(no changes)' is still a tick), never deferred mid-task; loop ends only on explicit operator stop OR daemon down AND no active agents on TWO consecutive ticks (single early 'no active agents' = dispatch lag); nested loops forbidden (.claude/skills/aiur-monitor/SKILL.md:49-91). Tests: none. Risk: high. Check: during a live run, verify a board post lands at every interval including steady-state '(no changes)' ticks.
- **FI-SKL-041 · Real-time alert relay protocol (push half)** [alert] — Armed ONCE per session via Monitor persistent:true running watch-alerts.sh; each JSON line posts in chat as '#<ticket> · <name> · <reason>'; NEW needs_attention:true alerts additionally relay via PushNotification ('#<ticket> · <agent> · <reason> — needs you'); never re-arm per tick, never push the same alert twice, informational alerts chat-only; additive to the /loop cadence (.claude/skills/aiur-monitor/SKILL.md:93-121). Tests: none for the relay protocol (script covered by src/test/aiur/aiur_alert_watch_skill_test.exs). Risk: high. Check: emit a needs_attention:true alert from a test agent and confirm one chat line plus exactly one PushNotification.
- **FI-SKL-042 · watch-alerts.sh alert streaming (cursors, baseline, exactly-once)** [other] — Long-lived bash watcher polling every agent's logs/agent.ndjson (find -maxdepth 6 across workspace roots), one JSON line per NEW alert, oldest→newest; in-memory per-file cursors, startup baseline skips history while post-startup files emit from zero; shrunk/rotated files re-baseline; a trailing half-written line is excluded until complete (neither emitted nor skipped); snapshot read once per scan. Portable to macOS bash 3.2 — no associative arrays (.claude/skills/aiur-monitor/scripts/watch-alerts.sh:163-232). Tests: src/test/aiur/aiur_alert_watch_skill_test.exs. Risk: medium.
- **FI-SKL-043 · watch-alerts.sh output schema + field derivation** [api] — Each line is {timestamp, ticket, source_ticket_id, agent, reason, severity, topic, name, needs_attention} with defined fallbacks (ticket: source_ticket_id → ticket.<id>. name prefix → workspace dirname; reason: message → last topic segment; severity default 'info'; agent = workspace dirname); AIUR_ALERT_NEEDS_ATTENTION=1 filters; works with or without jq (sed/grep escape fallback). Mirrors the #651/#662 agent.ndjson alert schema (.claude/skills/aiur-monitor/scripts/watch-alerts.sh:113-161,25-29). Tests: src/test/aiur/aiur_alert_watch_skill_test.exs:108-123,196-216. Risk: medium.
- **FI-SKL-044 · watch-alerts.sh config + workspace-root resolution** [config-option] — Optional config-path argument defaulting to ./.aiur/config with ./.aiurconfig legacy fallback; awk-parses workspace: root: (with ~ expansion) and ALWAYS also scans $HOME/.aiur/workspaces, deduplicating and skipping non-existent roots — alerts flow from BOTH roots. Duplicates aiur's config parsing in awk; renaming workspace.root silently zeroes the stream (.claude/skills/aiur-monitor/scripts/watch-alerts.sh:44-57,63-84). Tests: src/test/aiur/aiur_alert_watch_skill_test.exs. Risk: medium.
- **FI-SKL-045 · aiur-loop Step-0 involvement interview** [skill] — Before launching anything, six AskUserQuestion questions recorded for the session: scope, new-ticket filing, per-PR review involvement, merge authority, self-fix permission, cadence+concurrency (interval 5/10/15 default 5, agent count, ramp); unspecified answers default to the LESS autonomous option; no merging or unauthorized ticket-filing without confirmation (.claude/skills/aiur-loop/SKILL.md:16-40). Tests: none. Risk: high. Check: invoke /aiur-loop and confirm all six interview questions are asked before any launch command runs.
- **FI-SKL-046 · aiur-loop sustained improvement loop** [skill] — Repo-agnostic orchestration of aiur-run + aiur-monitor: launch --debug on a clean slate (no stray BEAM, dashboard port free, epmd clear), monitor at cadence, curate agent:todo if opted in, review each PR at the agreed level with findings commented on the LINKED TICKET (verifying comment→agent:rework→fix fires initially), merge at agreed authority then prove the change works, periodically stop/rebuild/rerun; merge in batches at rebuild checkpoints (interacts with #720) (.claude/skills/aiur-loop/SKILL.md:42-83). Tests: none. Risk: high. Check: post review findings on a linked ticket and confirm it flips to agent:rework and the agent picks up the fixes.
- **FI-SKL-047 · aiur-loop /goal cross-session resume** [skill] — When low on context, write a 2-sentence /goal capturing remaining scope + standing intent, tell the developer to paste it back via /goal in a fresh session, updating any handoff doc alongside (.claude/skills/aiur-loop/SKILL.md:71-75). Tests: none. Risk: low.
- **FI-SKL-048 · release skill workflow** [skill] — npm-only releases (Homebrew bump workflow disabled): pre-flight (block dirty tree, warn off-main, show latest tag + commits since), ask v<d>.<d>.<d> version + verify tag absent, bump src/mix.exs (single version source read via Mix.Project.config()[:version]), rebuild escript + verify ./bin/aiur --version, commit, annotated tag + push, gh release create with explicit --repo aiur-team/aiur; release-npm workflow publishes on the v* tag (.claude/skills/release/SKILL.md:8-10,14-73). Tests: none. Risk: medium.
- **FI-SKL-049 · release re-release + edge-case handling** [skill] — Recreating a version: gh release delete --yes, delete local tag, push tag deletion (git push origin :refs/tags/VERSION), re-tag. Contractual edge cases: dirty tree blocks; off-main warns but allows on confirmation; existing tag offers delete-recreate or new version; missing gh falls back to manual instructions (.claude/skills/release/SKILL.md:75-91). Tests: none. Risk: low.
- **FI-SKL-050 · Bundled skill install into agent workspaces (Aiur.AgentSkills.install/1)** [lifecycle] — After workspace population (warm-base materialize or cold clone), Aiur writes the bundled using-aiur + aiur-agent skills into <workspace>/.claude/skills/ so agents on non-aiur repos can load them (#689). Skill files are embedded at COMPILE time (@external_resource + File.read! keyed by relative path) so they survive into the shipped OTP release — editing .claude/skills/* requires recompiling aiur before workspaces see it; nothing reads the source tree at runtime (src/lib/aiur/agent_skills.ex:38-60,73-81; src/lib/aiur/workspace.ex:27,42). Tests: src/test/aiur/agent_skills_test.exs; src/test/aiur/aiur_agent_skill_test.exs:59-75. Risk: high.
- **FI-SKL-051 · Skill install safety properties (idempotent, no-clobber, best-effort, atomic)** [lifecycle] — install/1 is presence-checked per skill (idempotent), never overwrites a skill dir the target repo already ships (no-clobber — aiur dogfooding aiur keeps its own copy), rescues any ArgumentError/ErlangError/File.Error to :ok with an 'agent skill install failed' log (nil/non-binary workspace = no-op), and stages into a sibling <dest>.tmp.<uniq> dir renamed into place with after-block cleanup so interrupted writes never leave a half-installed skill (src/lib/aiur/agent_skills.ex:73-117). Tests: src/test/aiur/agent_skills_test.exs:50-99. Risk: medium.
- **FI-SKL-052 · Codex skill mirror in workspaces (relative symlinks, lstat guard)** [integration] — Each installed skill gets a symlink <workspace>/.codex/skills/<skill> → ../../.claude/skills/<skill> (File.ln_s!) so Codex agents discover the same canonical copy with no second copy to drift; presence check uses File.lstat (not File.exists?, which follows links) so a dangling Codex link reads as present and is left alone rather than crashing (src/lib/aiur/agent_skills.ex:129-143). Tests: src/test/aiur/agent_skills_test.exs:29-39,78-88 (exact relative target asserted). Risk: medium.
- **FI-SKL-053 · Local-worker-only skill install (remote worker_host no-op)** [lifecycle] — Workspace.create_for_issue calls AgentSkills.install only when worker_host is nil — remote workers materialize on another host where local writes wouldn't land, so remote workspaces intentionally get NO bundled skills; install runs after ensure_workspace + after_create hook inside the success path and never fails workspace creation (src/lib/aiur/workspace.ex:22-29,37-43). Tests: none directly (call-site gating untested). Risk: medium.
- **FI-SKL-054 · Operator-only vs issue-worker skill taxonomy** [other] — Two enforced families: issue-worker skills installed into every workspace (@issue_worker_skills = using-aiur, aiur-agent) vs operator-only skills never installed (aiur-run, aiur-monitor, aiur-loop, release — workers must not be advertised run/release authority); test-time cross-check asserts @codex_exposed_aiur_skills ∪ @claude_operator_only_skills = ALL dirs under .claude/skills/, so adding a 7th skill forces an explicit exposure decision (src/lib/aiur/agent_skills.ex:32-38; src/test/aiur/aiur_agent_skill_test.exs:26-27,59-94). Tests: src/test/aiur/agent_skills_test.exs:41-48; src/test/aiur/aiur_agent_skill_test.exs:59-94. Risk: low.
- **FI-SKL-055 · Repo-tree Codex exposure symlinks (.codex/skills)** [integration] — The aiur repo commits .codex/skills/{aiur-agent,aiur-monitor,aiur-run,using-aiur} as relative symlinks (../../.claude/skills/<skill>) so Codex agents working IN the aiur repo resolve the canonical Claude copies; aiur-loop and release deliberately have NO .codex entry; committed and runtime-generated links share the same relative shape, keeping checkouts and provisioned workspaces identical for Codex discovery (src/test/aiur/aiur_agent_skill_test.exs:47-57,88-93,189-202). Tests: src/test/aiur/aiur_agent_skill_test.exs:47-57,77-94 (symlink type, exact target, byte-identical SKILL.md). Risk: low.
- **FI-SKL-056 · Codex commit skill** [codex-skill-prompt] — Codex-native 12-step commit flow: read session history for intent, inspect status/diffs, stage with git add -A after confirming scope, flag suspicious new files before committing, conventional type(scope) prefix, imperative subject ≤72 chars, Summary/Rationale/Tests body wrapped at 72, no model/tool attribution trailer, commit via git commit -F (never -m with \n); refuses to commit when message and staged diff diverge. Convention deliberately differs from the Claude-side FI-SKL-016 (.codex/skills/commit/SKILL.md:24-50,60-75). Tests: none. Risk: low.
- **FI-SKL-057 · Codex debug skill (log-tracing runbook)** [codex-skill-prompt] — Runbook for stuck/failing runs tracing log/aiur.log* via three correlation keys (issue_identifier, issue_id Linear UUID, session_id <thread_id>-<turn_id>); prescribes triage order, ready-made rg commands, and a failure taxonomy: stall loop ('Issue stalled ... restarting with backoff'), app-server startup failure ('Codex session failed'), turn execution failure (turn_failed/turn_cancelled/turn_timeout), worker crash ('Agent task exited'); defers to src/docs/logging.md for required fields. Drift: doesn't mention persisted ~/.aiur/logs/<session>/ (PR #256) (.codex/skills/debug/SKILL.md:19-32,46-61,72-78). Tests: none. Risk: low.
- **FI-SKL-058 · Codex land skill + land_watch.py** [codex-skill-prompt+script] — End-to-end PR landing: keep conflict-free with main, keep CI green, address feedback, squash-merge (gh pr merge --squash with PR title/body), don't yield until merged; '## Codex Review — <persona>' comments each get a '[codex]'-prefixed accept/clarify/push-back reply BEFORE code changes; inline replies via REST with numeric in_reply_to; never merge with outstanding comments; never enable auto-merge (no required checks). land_watch.py (621 lines) polls every 10s (429-only backoff, max 5 retries; CODEX_BOTS allowlist) for review feedback, check runs, head-SHA changes, and conflicts — exit codes 2/3/4/5 (.codex/skills/land/SKILL.md:10-17,97-135; .codex/skills/land/land_watch.py:10-19,39-62,575-616). Tests: none — 621 untested polling lines; conflicts with the Claude-side never-merge-without-approval rule. Risk: high. Check: from an open test PR branch, run `python3 .codex/skills/land/land_watch.py` and confirm it exits 2 on an unanswered Codex review comment and 3 on a failed check.
- **FI-SKL-059 · Codex linear skill (linear_graphql usage guide)** [codex-skill-prompt] — Guide for the linear_graphql app-server tool (reuses Aiur's Linear auth): one operation per call; top-level errors array = failure even on 200; narrowest lookup first (key → identifier filter → id); fetch exact stateId before issueUpdate transitions; prefer attachmentLinkGitHubPR for PRs; never introduce raw-token shell helpers (shell curl PUT only for signed fileUpload URLs); ships copy-paste GraphQL incl. the 3-step file-upload flow (.codex/skills/linear/SKILL.md:13-33,242-296,338-389). Tests: none for the doc (tool tested Elixir-side). Risk: low.
- **FI-SKL-060 · Codex pull skill (merge-not-rebase update)** [codex-skill-prompt] — Branch update: verify clean tree, enable rerere (rerere.enabled + rerere.autoupdate — note autoupdate can silently replay a wrong recorded resolution), fetch, ff-only self-pull first (absorbs remote auto-commits), then git -c merge.conflictstyle=zdiff3 merge origin/main; conflict doctrine (inspect :1:/:2:/:3: stages, state each side's intent, minimal edits, tests per batch, handwritten before generated, git diff --check); asks the user only for uninferable intent/contract risk/equal designs/data loss/wrong target (.codex/skills/pull/SKILL.md:11-100). Tests: none. Risk: medium.
- **FI-SKL-061 · Codex push skill (safe push + PR create/update)** [codex-skill-prompt] — Validate locally, git push -u origin HEAD; on rejection, distinguish sync problems (delegate to pull skill, retry; --force-with-lease only after history rewrite, plain --force banned) from auth/permission failures (stop and surface, never rewrite remotes/protocols); MERGED/CLOSED PR ⇒ new branch + PR; body written against .github/pull_request_template.md, then verified via gh pr view --json body + `(cd src && mix pr_body.check --file <tmp>)`. STALE: validation gate written as `make -C elixir all` but the project lives under src/ (.codex/skills/push/SKILL.md:28-56,102-105,113). Tests: indirect via src/test/mix/tasks/pr_body_check_test.exs. Risk: medium.
- **FI-SKL-062 · Codex worktree_init.sh bootstrap** [bootstrap-script] — 16-line mise-based bootstrap for Codex worktrees: resolve repo root from script location, cd <root>/src, hard-fail with install pointer if mise absent, mise trust, make setup (→ mix deps.get via the setup alias); set -eo pipefail; deliberately no compile/test — complements the warm-base image for cold worktrees (.codex/worktree_init.sh:4-16; src/mix.exs:169). Tests: none. Risk: low.
