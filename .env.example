# aiur reads secrets from this file. Copy to .env and fill in values.
# Keep .env out of version control (it is gitignored).
#
# GitHub personal access token (repo scope). Create one at:
#   https://github.com/settings/tokens
GITHUB_TOKEN=
