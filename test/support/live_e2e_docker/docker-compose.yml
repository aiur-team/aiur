services:
  worker1:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${AIUR_LIVE_DOCKER_WORKER_1_PORT}:22"
    volumes:
      - ${AIUR_LIVE_DOCKER_AUTHORIZED_KEY}:/run/aiur/ssh/authorized_key.pub:ro
      - ${AIUR_LIVE_DOCKER_AUTH_JSON}:/root/.codex/auth.json:ro

  worker2:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${AIUR_LIVE_DOCKER_WORKER_2_PORT}:22"
    volumes:
      - ${AIUR_LIVE_DOCKER_AUTHORIZED_KEY}:/run/aiur/ssh/authorized_key.pub:ro
      - ${AIUR_LIVE_DOCKER_AUTH_JSON}:/root/.codex/auth.json:ro
