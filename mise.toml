[tools]
erlang = "28"
elixir = "1.19.5-otp-28"
opencode = "1.15.6"            # pinned: aiur's opencode pane integration is validated against 1.15.6 (1.17.x broke custom-provider model resolution)
ripgrep = "latest"

[tasks.setup]
description = "Bootstrap the dev environment: install the toolchain and symlink aiurdev onto your PATH"
run = "$MISE_PROJECT_ROOT/scripts/setup"
