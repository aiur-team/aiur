#!/usr/bin/env bash
# Materialize run-summaries (and optional build rollups) into the per-repo state node.
set -euo pipefail
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="$script_dir/lib${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m analytics.reduce_cmd "$@"
