{"_comment":"Pinned ticket IDs that `aiur --test` is allowed to operate on. The CLI refuses to run if the workflow YAML's repo doesn't match the expected_remote below. These tickets are the 3 sandbox tickets that the events-foundation manual test exercises.","expected_remote":"git@github.com:its-everdred/aiur.git","golden_ticket":212,"tickets":[99,100,101]}